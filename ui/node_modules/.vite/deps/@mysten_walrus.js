import "./chunk-EGRHWZRV.js";
import "./chunk-4EPHXCPH.js";
import {
  Transaction,
  coinWithBalance
} from "./chunk-QMR7PTWI.js";
import {
  ClientCache
} from "./chunk-AYK64H6I.js";
import {
  deriveDynamicFieldID,
  isArgument
} from "./chunk-CHJ7WHJI.js";
import "./chunk-VRMCH4GN.js";
import {
  BcsEnum,
  BcsStruct,
  BcsTuple,
  TypeTagSerializer,
  bcs,
  chunk,
  fromBase64,
  normalizeStructTag,
  normalizeSuiAddress,
  parseStructTag,
  pureBcsSchemaFromTypeName,
  suiBcs,
  toBase64
} from "./chunk-HB2WJ3UT.js";
import {
  __commonJS,
  __privateAdd,
  __privateGet,
  __privateMethod,
  __privateSet,
  __toESM
} from "./chunk-QWN5BXRD.js";

// ../node_modules/dataloader/index.js
var require_dataloader = __commonJS({
  "../node_modules/dataloader/index.js"(exports, module) {
    "use strict";
    var DataLoader2 = (function() {
      function DataLoader3(batchLoadFn, options) {
        if (typeof batchLoadFn !== "function") {
          throw new TypeError("DataLoader must be constructed with a function which accepts " + ("Array<key> and returns Promise<Array<value>>, but got: " + batchLoadFn + "."));
        }
        this._batchLoadFn = batchLoadFn;
        this._maxBatchSize = getValidMaxBatchSize(options);
        this._batchScheduleFn = getValidBatchScheduleFn(options);
        this._cacheKeyFn = getValidCacheKeyFn(options);
        this._cacheMap = getValidCacheMap(options);
        this._batch = null;
        this.name = getValidName(options);
      }
      var _proto = DataLoader3.prototype;
      _proto.load = function load(key) {
        if (key === null || key === void 0) {
          throw new TypeError("The loader.load() function must be called with a value, " + ("but got: " + String(key) + "."));
        }
        var batch = getCurrentBatch(this);
        var cacheMap = this._cacheMap;
        var cacheKey;
        if (cacheMap) {
          cacheKey = this._cacheKeyFn(key);
          var cachedPromise = cacheMap.get(cacheKey);
          if (cachedPromise) {
            var cacheHits = batch.cacheHits || (batch.cacheHits = []);
            return new Promise(function(resolve) {
              cacheHits.push(function() {
                resolve(cachedPromise);
              });
            });
          }
        }
        batch.keys.push(key);
        var promise = new Promise(function(resolve, reject) {
          batch.callbacks.push({
            resolve,
            reject
          });
        });
        if (cacheMap) {
          cacheMap.set(cacheKey, promise);
        }
        return promise;
      };
      _proto.loadMany = function loadMany(keys) {
        if (!isArrayLike(keys)) {
          throw new TypeError("The loader.loadMany() function must be called with Array<key> " + ("but got: " + keys + "."));
        }
        var loadPromises = [];
        for (var i = 0; i < keys.length; i++) {
          loadPromises.push(this.load(keys[i])["catch"](function(error) {
            return error;
          }));
        }
        return Promise.all(loadPromises);
      };
      _proto.clear = function clear(key) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          cacheMap["delete"](cacheKey);
        }
        return this;
      };
      _proto.clearAll = function clearAll() {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          cacheMap.clear();
        }
        return this;
      };
      _proto.prime = function prime(key, value) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          if (cacheMap.get(cacheKey) === void 0) {
            var promise;
            if (value instanceof Error) {
              promise = Promise.reject(value);
              promise["catch"](function() {
              });
            } else {
              promise = Promise.resolve(value);
            }
            cacheMap.set(cacheKey, promise);
          }
        }
        return this;
      };
      return DataLoader3;
    })();
    var enqueuePostPromiseJob = typeof process === "object" && typeof process.nextTick === "function" ? function(fn) {
      if (!resolvedPromise) {
        resolvedPromise = Promise.resolve();
      }
      resolvedPromise.then(function() {
        process.nextTick(fn);
      });
    } : typeof setImmediate === "function" ? function(fn) {
      setImmediate(fn);
    } : function(fn) {
      setTimeout(fn);
    };
    var resolvedPromise;
    function getCurrentBatch(loader) {
      var existingBatch = loader._batch;
      if (existingBatch !== null && !existingBatch.hasDispatched && existingBatch.keys.length < loader._maxBatchSize) {
        return existingBatch;
      }
      var newBatch = {
        hasDispatched: false,
        keys: [],
        callbacks: []
      };
      loader._batch = newBatch;
      loader._batchScheduleFn(function() {
        dispatchBatch(loader, newBatch);
      });
      return newBatch;
    }
    function dispatchBatch(loader, batch) {
      batch.hasDispatched = true;
      if (batch.keys.length === 0) {
        resolveCacheHits(batch);
        return;
      }
      var batchPromise;
      try {
        batchPromise = loader._batchLoadFn(batch.keys);
      } catch (e) {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function " + ("errored synchronously: " + String(e) + ".")));
      }
      if (!batchPromise || typeof batchPromise.then !== "function") {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise: " + String(batchPromise) + ".")));
      }
      batchPromise.then(function(values) {
        if (!isArrayLike(values)) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise of an Array: " + String(values) + "."));
        }
        if (values.length !== batch.keys.length) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did not return a Promise of an Array of the same length as the Array of keys." + ("\n\nKeys:\n" + String(batch.keys)) + ("\n\nValues:\n" + String(values)));
        }
        resolveCacheHits(batch);
        for (var i = 0; i < batch.callbacks.length; i++) {
          var _value = values[i];
          if (_value instanceof Error) {
            batch.callbacks[i].reject(_value);
          } else {
            batch.callbacks[i].resolve(_value);
          }
        }
      })["catch"](function(error) {
        failedDispatch(loader, batch, error);
      });
    }
    function failedDispatch(loader, batch, error) {
      resolveCacheHits(batch);
      for (var i = 0; i < batch.keys.length; i++) {
        loader.clear(batch.keys[i]);
        batch.callbacks[i].reject(error);
      }
    }
    function resolveCacheHits(batch) {
      if (batch.cacheHits) {
        for (var i = 0; i < batch.cacheHits.length; i++) {
          batch.cacheHits[i]();
        }
      }
    }
    function getValidMaxBatchSize(options) {
      var shouldBatch = !options || options.batch !== false;
      if (!shouldBatch) {
        return 1;
      }
      var maxBatchSize = options && options.maxBatchSize;
      if (maxBatchSize === void 0) {
        return Infinity;
      }
      if (typeof maxBatchSize !== "number" || maxBatchSize < 1) {
        throw new TypeError("maxBatchSize must be a positive number: " + maxBatchSize);
      }
      return maxBatchSize;
    }
    function getValidBatchScheduleFn(options) {
      var batchScheduleFn = options && options.batchScheduleFn;
      if (batchScheduleFn === void 0) {
        return enqueuePostPromiseJob;
      }
      if (typeof batchScheduleFn !== "function") {
        throw new TypeError("batchScheduleFn must be a function: " + batchScheduleFn);
      }
      return batchScheduleFn;
    }
    function getValidCacheKeyFn(options) {
      var cacheKeyFn = options && options.cacheKeyFn;
      if (cacheKeyFn === void 0) {
        return function(key) {
          return key;
        };
      }
      if (typeof cacheKeyFn !== "function") {
        throw new TypeError("cacheKeyFn must be a function: " + cacheKeyFn);
      }
      return cacheKeyFn;
    }
    function getValidCacheMap(options) {
      var shouldCache = !options || options.cache !== false;
      if (!shouldCache) {
        return null;
      }
      var cacheMap = options && options.cacheMap;
      if (cacheMap === void 0) {
        return /* @__PURE__ */ new Map();
      }
      if (cacheMap !== null) {
        var cacheFunctions = ["get", "set", "delete", "clear"];
        var missingFunctions = cacheFunctions.filter(function(fnName) {
          return cacheMap && typeof cacheMap[fnName] !== "function";
        });
        if (missingFunctions.length !== 0) {
          throw new TypeError("Custom cacheMap missing methods: " + missingFunctions.join(", "));
        }
      }
      return cacheMap;
    }
    function getValidName(options) {
      if (options && options.name) {
        return options.name;
      }
      return null;
    }
    function isArrayLike(x) {
      return typeof x === "object" && x !== null && typeof x.length === "number" && (x.length === 0 || x.length > 0 && Object.prototype.hasOwnProperty.call(x, x.length - 1));
    }
    module.exports = DataLoader2;
  }
});

// ../node_modules/@mysten/walrus/dist/constants.mjs
var TESTNET_WALRUS_PACKAGE_CONFIG = {
  systemObjectId: "0x6c2547cbbc38025cf3adac45f63cb0a8d12ecf777cdc75a4971612bf97fdf6af",
  stakingPoolId: "0xbe46180321c30aab2f8b3501e24048377287fa708018a5b7c2792b35fe339ee3",
  exchangeIds: [
    "0xf4d164ea2def5fe07dc573992a029e010dba09b1a8dcbc44c5c2e79567f39073",
    "0x19825121c52080bb1073662231cfea5c0e4d905fd13e95f21e9a018f2ef41862",
    "0x83b454e524c71f30803f4d6c302a86fb6a39e96cdfb873c2d1e93bc1c26a3bc5",
    "0x8d63209cf8589ce7aef8f262437163c67577ed09f3e636a9d8e0813843fb8bf1"
  ]
};
var MAINNET_WALRUS_PACKAGE_CONFIG = {
  systemObjectId: "0x2134d52768ea07e8c43570ef975eb3e4c27a39fa6396bef985b5abc58d03ddd2",
  stakingPoolId: "0x10b9d30c28448939ce6c4d6c6e0ffce4a7f8a4ada8248bdad09ef8b70e4a3904"
};
var SLIVER_STATUS_CHECK_THRESHOLD = 5560;
var statusLifecycleRank = {
  nonexistent: 0,
  deletable: 1,
  permanent: 2,
  invalid: 3
};

// ../node_modules/@mysten/walrus/dist/error.mjs
var WalrusClientError = class extends Error {
};
var RetryableWalrusClientError = class extends WalrusClientError {
};
var NoBlobStatusReceivedError = class extends WalrusClientError {
};
var NoVerifiedBlobStatusReceivedError = class extends WalrusClientError {
};
var NoBlobMetadataReceivedError = class extends RetryableWalrusClientError {
};
var NotEnoughSliversReceivedError = class extends RetryableWalrusClientError {
};
var NotEnoughBlobConfirmationsError = class extends RetryableWalrusClientError {
};
var BehindCurrentEpochError = class extends RetryableWalrusClientError {
};
var BlobNotCertifiedError = class extends RetryableWalrusClientError {
};
var InconsistentBlobError = class extends WalrusClientError {
};
var BlobBlockedError = class extends Error {
};

// ../node_modules/@mysten/walrus/dist/utils/bcs.mjs
var MerkleNode = suiBcs.enum("MerkleNode", {
  Empty: null,
  Digest: suiBcs.bytes(32)
});
var SliverPairMetadata = suiBcs.struct("SliverPairMetadata", {
  primary_hash: MerkleNode,
  secondary_hash: MerkleNode
});
var EncodingType = suiBcs.enum("EncodingType", {
  RedStuff: null,
  RS2: null
}).transform({
  input: (encodingType) => typeof encodingType === "string" ? { [encodingType]: null } : encodingType,
  output: (encodingType) => encodingType
});
var BlobMetadataV1 = suiBcs.struct("BlobMetadataV1", {
  encoding_type: EncodingType,
  unencoded_length: suiBcs.u64(),
  hashes: suiBcs.vector(SliverPairMetadata)
});
var BlobMetadata = suiBcs.enum("BlobMetadata", { V1: BlobMetadataV1 });
var BlobId = suiBcs.u256().transform({
  input: (blobId) => typeof blobId === "string" ? blobIdToInt(blobId) : blobId,
  output: (id) => blobIdFromInt(id)
});
function blobIdFromInt(blobId) {
  return suiBcs.u256().serialize(blobId).toBase64().replace(/=*$/, "").replaceAll("+", "-").replaceAll("/", "_");
}
function blobIdFromBytes(blobId) {
  return blobIdFromInt(suiBcs.u256().parse(blobId));
}
function blobIdToInt(blobId) {
  return BigInt(suiBcs.u256().fromBase64(blobId.replaceAll("-", "+").replaceAll("_", "/")));
}
var BlobMetadataWithId = suiBcs.struct("BlobMetadataWithId", {
  blobId: BlobId,
  metadata: BlobMetadata
});
var Symbols = suiBcs.struct("Symbols", {
  data: suiBcs.byteVector(),
  symbol_size: suiBcs.u16()
});
var SliverData = suiBcs.struct("SliverData", {
  symbols: Symbols,
  index: suiBcs.u16()
});
var Sliver = suiBcs.enum("Sliver", {
  Primary: SliverData,
  Secondary: SliverData
});
var SliverPair = suiBcs.struct("SliverPair", {
  primary: SliverData,
  secondary: SliverData
});
var IntentType = (function(IntentType$1) {
  IntentType$1[IntentType$1["PROOF_OF_POSSESSION_MSG"] = 0] = "PROOF_OF_POSSESSION_MSG";
  IntentType$1[IntentType$1["BLOB_CERT_MSG"] = 1] = "BLOB_CERT_MSG";
  IntentType$1[IntentType$1["INVALID_BLOB_ID_MSG"] = 2] = "INVALID_BLOB_ID_MSG";
  IntentType$1[IntentType$1["SYNC_SHARD_MSG"] = 3] = "SYNC_SHARD_MSG";
  return IntentType$1;
})({});
var Intent = suiBcs.struct("Intent", {
  type: suiBcs.u8().transform({
    input: (type) => type,
    output: (type) => type
  }),
  version: suiBcs.u8(),
  appId: suiBcs.u8()
}).transform({
  input: (intent) => ({
    type: intent,
    version: 0,
    appId: 3
  }),
  output: (intent) => intent.type
});
function ProtocolMessage(messageContents) {
  return suiBcs.struct(`ProtocolMessage<${messageContents.name}>`, {
    intent: Intent,
    epoch: suiBcs.u32(),
    messageContents
  });
}
var BlobPersistenceType = suiBcs.enum("BlobPersistenceType", {
  Permanent: null,
  Deletable: suiBcs.struct("Deletable", { objectId: suiBcs.Address })
});
var StorageConfirmationBody = suiBcs.struct("StorageConfirmationBody", {
  blobId: BlobId,
  blobType: BlobPersistenceType
});
var StorageConfirmation = ProtocolMessage(StorageConfirmationBody);
function Field(...typeParameters) {
  return suiBcs.struct("Field", {
    id: suiBcs.Address,
    name: typeParameters[0],
    value: typeParameters[1]
  });
}
var QuiltPatchTags = suiBcs.map(suiBcs.string(), suiBcs.string()).transform({
  input: (tags) => tags instanceof Map ? tags : new Map(Object.entries(tags)),
  output: (tags) => Object.fromEntries(tags)
});
var QuiltPatchV1 = suiBcs.struct("QuiltPatchV1", {
  endIndex: suiBcs.u16(),
  identifier: suiBcs.string(),
  tags: QuiltPatchTags
});
var QuiltIndexV1 = suiBcs.struct("QuiltIndexV1", { patches: suiBcs.vector(QuiltPatchV1) });
var QuiltPatchId = suiBcs.struct("QuiltPatchId", {
  quiltId: BlobId,
  patchId: suiBcs.struct("InternalQuiltPatchId", {
    version: suiBcs.u8(),
    startIndex: suiBcs.u16(),
    endIndex: suiBcs.u16()
  })
});
var QuiltPatchBlobHeader = suiBcs.struct("QuiltPatchBlobHeader", {
  version: suiBcs.u8(),
  length: suiBcs.u32(),
  mask: suiBcs.u8()
});
var CertificateBcs = suiBcs.struct("Certificate", {
  signers: suiBcs.vector(suiBcs.u16()),
  serializedMessage: suiBcs.byteVector(),
  signature: suiBcs.byteVector()
});
function parseCertificateFromBase64(base64) {
  const cert = CertificateBcs.fromBase64(base64);
  return {
    signers: cert.signers,
    serializedMessage: new Uint8Array(cert.serializedMessage),
    signature: new Uint8Array(cert.signature)
  };
}

// ../node_modules/@mysten/walrus/dist/storage-node/error.mjs
var StorageNodeError = class extends Error {
};
var _StorageNodeAPIError_static, _a, makeMessage_fn;
var StorageNodeAPIError = (_a = class extends StorageNodeError {
  constructor(status, error, message) {
    var _a12;
    super(__privateMethod(_a12 = _a, _StorageNodeAPIError_static, makeMessage_fn).call(_a12, status, error, message));
    this.status = status;
    this.error = error;
  }
  static generate(status, errorResponse, message) {
    if (!status) return new ConnectionError({ message });
    if (status === 400) return BadRequestError.generate(status, errorResponse, message);
    if (status === 401) return new AuthenticationError(status, errorResponse, message);
    if (status === 403) return new PermissionDeniedError(status, errorResponse, message);
    if (status === 404) return new NotFoundError(status, errorResponse, message);
    if (status === 409) return new ConflictError(status, errorResponse, message);
    if (status === 422) return new UnprocessableEntityError(status, errorResponse, message);
    if (status === 429) return new RateLimitError(status, errorResponse, message);
    if (status === 451) return new LegallyUnavailableError(status, errorResponse, message);
    if (status >= 500) return new InternalServerError(status, errorResponse, message);
    return new _a(status, errorResponse, message);
  }
}, _StorageNodeAPIError_static = new WeakSet(), makeMessage_fn = function(status, error, message) {
  function hasErrorMessage(error$1) {
    var _a12;
    return typeof ((_a12 = error$1 == null ? void 0 : error$1.error) == null ? void 0 : _a12.message) === "string";
  }
  const inferredMessage = hasErrorMessage(error) ? error.error.message : message;
  const finalMessage = inferredMessage ? inferredMessage : JSON.stringify(error);
  if (status && finalMessage) return `${status} ${finalMessage}`;
  else if (finalMessage) return finalMessage;
  else if (status) return `${status} status code (no body)`;
  return "(no status code or body)";
}, __privateAdd(_a, _StorageNodeAPIError_static), _a);
var UserAbortError = class extends StorageNodeAPIError {
  constructor({ message } = {}) {
    super(void 0, void 0, message || "Request was aborted.");
  }
};
var ConnectionError = class extends StorageNodeAPIError {
  constructor({ message }) {
    super(void 0, void 0, message || "Connection error.");
  }
};
var ConnectionTimeoutError = class extends StorageNodeAPIError {
  constructor({ message } = {}) {
    super(void 0, void 0, message ?? "Request timed out.");
  }
};
var BadRequestError = class BadRequestError2 extends StorageNodeAPIError {
  static generate(status, errorResponse, message) {
    var _a12, _b;
    if (errorResponse && typeof errorResponse === "object" && "error" in errorResponse) {
      if (((_b = (_a12 = errorResponse.error.details) == null ? void 0 : _a12[0]) == null ? void 0 : _b.reason) === "NOT_REGISTERED") return new BlobNotRegisteredError(errorResponse, message);
    }
    return new BadRequestError2(status, errorResponse, message);
  }
};
var BlobNotRegisteredError = class extends StorageNodeAPIError {
  constructor(error, message) {
    super(400, error, message);
  }
};
var AuthenticationError = class extends StorageNodeAPIError {
};
var PermissionDeniedError = class extends StorageNodeAPIError {
};
var NotFoundError = class extends StorageNodeAPIError {
};
var ConflictError = class extends StorageNodeAPIError {
};
var UnprocessableEntityError = class extends StorageNodeAPIError {
};
var RateLimitError = class extends StorageNodeAPIError {
};
var LegallyUnavailableError = class extends StorageNodeAPIError {
};
var InternalServerError = class extends StorageNodeAPIError {
};

// ../node_modules/@mysten/walrus/dist/utils/index.mjs
var DIGEST_LEN = 32;
var BLOB_ID_LEN = 32;
var REQUIRED_ALIGNMENT_BY_ENCODING_TYPE = {
  RS2: 2,
  RedStuff: 2
};
var MAX_SYMBOL_SIZE_BY_ENCODING_TYPE = {
  RS2: 2 ** 16 - 1,
  RedStuff: 2 ** 16 - 1
};
function encodedBlobLength(unencodedLength, nShards, encodingType = "RS2") {
  const sliverSize = encodedSliverSize(unencodedLength, nShards, encodingType);
  return nShards * (nShards * DIGEST_LEN * 2 + BLOB_ID_LEN) + sliverSize;
}
function encodedSliverSize(unencodedLength, nShards, encodingType = "RS2") {
  const { primarySymbols, secondarySymbols } = getSourceSymbols(nShards, encodingType);
  let symbolSize = Math.floor((Math.max(unencodedLength, 1) - 1) / (primarySymbols * secondarySymbols)) + 1;
  if (encodingType === "RS2" && symbolSize % 2 === 1) symbolSize = symbolSize + 1;
  return (primarySymbols + secondarySymbols) * symbolSize * nShards;
}
function getSourceSymbols(nShards, encodingType = "RS2") {
  const safetyLimit = decodingSafetyLimit(nShards, encodingType);
  const maxFaulty = getMaxFaultyNodes(nShards);
  const minCorrect = nShards - maxFaulty;
  return {
    primarySymbols: minCorrect - maxFaulty - safetyLimit,
    secondarySymbols: minCorrect - safetyLimit
  };
}
function isQuorum(size, nShards) {
  return size > 2 * getMaxFaultyNodes(nShards);
}
function isAboveValidity(size, nShards) {
  return size > getMaxFaultyNodes(nShards);
}
function getMaxFaultyNodes(nShards) {
  return Math.floor((nShards - 1) / 3);
}
function decodingSafetyLimit(nShards, encodingType) {
  switch (encodingType) {
    case "RedStuff":
      return Math.min(5, Math.floor(getMaxFaultyNodes(nShards) / 5));
    case "RS2":
      return 0;
    default:
      throw new Error(`Encountered unknown encoding type of ${encodingType}`);
  }
}
var BYTES_PER_UNIT_SIZE = 1024 * 1024;
function storageUnitsFromSize(size) {
  return Math.ceil(size / BYTES_PER_UNIT_SIZE);
}
function rotationOffset(bytes, modulus) {
  return bytes.reduce((acc, byte) => (acc * 256 + byte) % modulus, 0);
}
function toShardIndex(sliverPairIndex, blobId, numShards) {
  return (sliverPairIndex + rotationOffset(BlobId.serialize(blobId).toBytes(), numShards)) % numShards;
}
function sliverPairIndexFromSecondarySliverIndex(sliverIndex, numShards) {
  return numShards - sliverIndex - 1;
}
function toPairIndex(shardIndex, blobId, numShards) {
  const offset = rotationOffset(BlobId.serialize(blobId).toBytes(), numShards);
  return (numShards + shardIndex - offset) % numShards;
}
function signersToBitmap(signers, committeeSize) {
  const bitmapSize = Math.ceil(committeeSize / 8);
  const bitmap = new Uint8Array(bitmapSize);
  for (const signer of signers) {
    const byteIndex = Math.floor(signer / 8);
    const bitIndex = signer % 8;
    bitmap[byteIndex] |= 1 << bitIndex;
  }
  return bitmap;
}
function getShardIndicesByNodeId(committee) {
  const shardIndicesByNodeId = /* @__PURE__ */ new Map();
  for (const node of committee[0].contents) {
    if (!shardIndicesByNodeId.has(node.key)) shardIndicesByNodeId.set(node.key, []);
    shardIndicesByNodeId.get(node.key).push(...node.value);
  }
  return shardIndicesByNodeId;
}
function urlSafeBase64(bytes) {
  return toBase64(bytes).replace(/=*$/, "").replaceAll("+", "-").replaceAll("/", "_");
}
function fromUrlSafeBase64(base64) {
  return fromBase64(base64.replaceAll("-", "+").replaceAll("_", "/"));
}
function getSizes(blobSize, numShards) {
  const encodedBlobSize = encodedSliverSize(blobSize, numShards);
  const { primarySymbols, secondarySymbols } = getSourceSymbols(numShards);
  const totalSymbols = (primarySymbols + secondarySymbols) * numShards;
  if (encodedBlobSize % totalSymbols !== 0) throw new Error("encoded blob size should be divisible by total symbols");
  const symbolSize = encodedBlobSize / totalSymbols;
  if (encodedBlobSize % totalSymbols !== 0) throw new Error("blob length should be divisible by total symbols");
  return {
    symbolSize,
    rowSize: symbolSize * secondarySymbols,
    columnSize: symbolSize * primarySymbols,
    blobSize
  };
}

// ../node_modules/@mysten/walrus/dist/utils/quilts.mjs
var QUILT_INDEX_SIZE_BYTES_LENGTH = 4;
var QUILT_VERSION_BYTES_LENGTH = 1;
var QUILT_INDEX_PREFIX_SIZE = QUILT_VERSION_BYTES_LENGTH + QUILT_INDEX_SIZE_BYTES_LENGTH;
var QUILT_PATCH_BLOB_HEADER_SIZE = 6;
var BLOB_IDENTIFIER_SIZE_BYTES_LENGTH = 2;
var TAGS_SIZE_BYTES_LENGTH = 2;
var MAX_BLOB_IDENTIFIER_BYTES_LENGTH = (1 << 8 * BLOB_IDENTIFIER_SIZE_BYTES_LENGTH) - 1;
var MAX_NUM_SLIVERS_FOR_QUILT_INDEX = 10;
var HAS_TAGS_FLAG = 1;
function computeSymbolSize(blobsSizes, nColumns, nRows, maxNumColumnsForQuiltIndex, encodingType = "RS2") {
  if (blobsSizes.length > nColumns) throw new Error("Too many blobs, the number of blobs must be less than the number of columns");
  if (blobsSizes.length === 0) throw new Error("No blobs provided");
  let minVal = Math.max(blobsSizes.reduce((acc, size) => acc + size, 0) / (nColumns * nRows), blobsSizes[0] / (nRows * maxNumColumnsForQuiltIndex), Math.ceil(QUILT_INDEX_PREFIX_SIZE / nRows));
  let maxVal = Math.ceil(Math.max(...blobsSizes) / (nColumns / blobsSizes.length) * nRows);
  while (minVal < maxVal) {
    const mid = Math.floor((minVal + maxVal) / 2);
    if (canBlobsFitIntoMatrix(blobsSizes, nColumns, mid * nRows)) maxVal = mid;
    else minVal = mid + 1;
  }
  const symbolSize = Math.ceil(minVal / REQUIRED_ALIGNMENT_BY_ENCODING_TYPE[encodingType]) * REQUIRED_ALIGNMENT_BY_ENCODING_TYPE[encodingType];
  if (!canBlobsFitIntoMatrix(blobsSizes, nColumns, symbolSize * nRows)) throw new Error("Quilt oversize");
  if (symbolSize > MAX_SYMBOL_SIZE_BY_ENCODING_TYPE[encodingType]) throw new Error(`Quilt oversize: the resulting symbol size ${symbolSize} is larger than the maximum symbol size ${MAX_SYMBOL_SIZE_BY_ENCODING_TYPE[encodingType]}; remove some blobs`);
  return symbolSize;
}
function canBlobsFitIntoMatrix(blobsSizes, nColumns, columnSize) {
  return blobsSizes.reduce((acc, size) => acc + Math.ceil(size / columnSize), 0) <= nColumns;
}
function parseQuiltPatchId(id) {
  return QuiltPatchId.parse(fromUrlSafeBase64(id));
}
function encodeQuiltPatchId(id) {
  return urlSafeBase64(QuiltPatchId.serialize(id).toBytes());
}
function parseWalrusId(id) {
  if (fromUrlSafeBase64(id).length === 32) return {
    kind: "blob",
    id
  };
  return {
    kind: "quiltPatch",
    id: parseQuiltPatchId(id)
  };
}
function encodeQuilt({ blobs, numShards, encodingType }) {
  const { primarySymbols: nRows, secondarySymbols: nCols } = getSourceSymbols(numShards, encodingType);
  const sortedBlobs = [...blobs].sort((a, b) => a.identifier < b.identifier ? -1 : 1);
  const identifiers = /* @__PURE__ */ new Set();
  const index = { patches: [] };
  const tags = sortedBlobs.map((blob) => blob.tags && Object.keys(blob.tags).length > 0 ? QuiltPatchTags.serialize(blob.tags).toBytes() : null);
  for (const blob of sortedBlobs) {
    if (identifiers.has(blob.identifier)) throw new Error(`Duplicate blob identifier: ${blob.identifier}`);
    identifiers.add(blob.identifier);
    index.patches.push({
      startIndex: 0,
      endIndex: 0,
      identifier: blob.identifier,
      tags: blob.tags ?? {}
    });
  }
  const indexSize = QUILT_INDEX_PREFIX_SIZE + QuiltIndexV1.serialize(index).toBytes().length;
  const blobMetadata = sortedBlobs.map((blob, i) => {
    const identifierBytes = bcs.string().serialize(blob.identifier).toBytes();
    let metadataSize = QUILT_PATCH_BLOB_HEADER_SIZE + BLOB_IDENTIFIER_SIZE_BYTES_LENGTH + identifierBytes.length;
    let mask = 0;
    let offset = 0;
    if (tags[i]) {
      metadataSize += TAGS_SIZE_BYTES_LENGTH + tags[i].length;
      mask |= HAS_TAGS_FLAG << 0;
    }
    const metadata = new Uint8Array(metadataSize);
    const metadataView = new DataView(metadata.buffer);
    const header = QuiltPatchBlobHeader.serialize({
      version: 1,
      length: metadataSize - QUILT_PATCH_BLOB_HEADER_SIZE + blob.contents.length,
      mask
    }).toBytes();
    metadata.set(header, offset);
    offset += header.length;
    metadataView.setUint16(offset, identifierBytes.length, true);
    offset += BLOB_IDENTIFIER_SIZE_BYTES_LENGTH;
    metadata.set(identifierBytes, offset);
    offset += identifierBytes.length;
    if (tags[i]) {
      metadataView.setUint16(offset, tags[i].length, true);
      offset += TAGS_SIZE_BYTES_LENGTH;
      metadata.set(tags[i], offset);
      offset += tags[i].length;
    }
    return metadata;
  });
  const symbolSize = computeSymbolSize([indexSize, ...sortedBlobs.map((blob, i) => {
    if (blob.identifier.length > MAX_BLOB_IDENTIFIER_BYTES_LENGTH) throw new Error(`Blob identifier too long: ${blob.identifier}`);
    return blobMetadata[i].length + blob.contents.length;
  })], nCols, nRows, MAX_NUM_SLIVERS_FOR_QUILT_INDEX, encodingType);
  const rowSize = symbolSize * nCols;
  const columnSize = symbolSize * nRows;
  const indexColumnsNeeded = Math.ceil(indexSize / columnSize);
  if (indexColumnsNeeded > MAX_NUM_SLIVERS_FOR_QUILT_INDEX) throw new Error("Index too large");
  const quilt = new Uint8Array(rowSize * nRows);
  let currentColumn = indexColumnsNeeded;
  for (let i = 0; i < sortedBlobs.length; i++) {
    const blob = sortedBlobs[i];
    index.patches[i].startIndex = currentColumn;
    currentColumn += writeBlobToQuilt(quilt, blob.contents, rowSize, columnSize, symbolSize, currentColumn, blobMetadata[i]);
    index.patches[i].endIndex = currentColumn;
  }
  const indexBytes = QuiltIndexV1.serialize(index).toBytes();
  const quiltIndex = new Uint8Array(QUILT_INDEX_PREFIX_SIZE + indexBytes.length);
  const view = new DataView(quiltIndex.buffer);
  view.setUint8(0, 1);
  view.setUint32(1, indexBytes.length, true);
  quiltIndex.set(indexBytes, QUILT_INDEX_PREFIX_SIZE);
  writeBlobToQuilt(quilt, quiltIndex, rowSize, columnSize, symbolSize, 0);
  return {
    quilt,
    index
  };
}
function writeBlobToQuilt(quilt, blob, rowSize, columnSize, symbolSize, startColumn, prefix) {
  const nRows = columnSize / symbolSize;
  let bytesWritten = 0;
  if (rowSize % symbolSize !== 0) throw new Error("Row size must be divisible by symbol size");
  if (columnSize % symbolSize !== 0) throw new Error("Column size must be divisible by symbol size");
  if (prefix) writeBytes(prefix);
  writeBytes(blob);
  return Math.ceil(bytesWritten / columnSize);
  function writeBytes(bytes) {
    const offset = bytesWritten;
    const symbolsToSkip = Math.floor(offset / symbolSize);
    let remainingOffset = offset % symbolSize;
    let currentCol = startColumn + Math.floor(symbolsToSkip / nRows);
    let currentRow = symbolsToSkip % nRows;
    let index = 0;
    while (index < bytes.length) {
      const startIndex = currentRow * rowSize + currentCol * symbolSize + remainingOffset;
      const len = Math.min(symbolSize - remainingOffset, bytes.length - index);
      for (let i = 0; i < len; i++) quilt[startIndex + i] = bytes[index + i];
      index += len;
      remainingOffset = 0;
      currentRow = (currentRow + 1) % nRows;
      if (currentRow === 0) currentCol++;
    }
    bytesWritten += bytes.length;
  }
}

// ../node_modules/@mysten/walrus/dist/files/readers/local.mjs
var _contents, _identifier, _tags, _a2;
var LocalReader = (_a2 = class {
  constructor({ contents, identifier, tags }) {
    __privateAdd(this, _contents);
    __privateAdd(this, _identifier);
    __privateAdd(this, _tags);
    __privateSet(this, _contents, contents);
    __privateSet(this, _identifier, identifier ?? null);
    __privateSet(this, _tags, tags ?? {});
  }
  async getBytes() {
    if ("arrayBuffer" in __privateGet(this, _contents)) return new Uint8Array(await __privateGet(this, _contents).arrayBuffer());
    return __privateGet(this, _contents);
  }
  async getIdentifier() {
    return __privateGet(this, _identifier);
  }
  async getTags() {
    return __privateGet(this, _tags);
  }
}, _contents = new WeakMap(), _identifier = new WeakMap(), _tags = new WeakMap(), _a2);

// ../node_modules/@mysten/walrus/dist/files/file.mjs
var _reader, _a3;
var WalrusFile = (_a3 = class {
  constructor({ reader }) {
    __privateAdd(this, _reader);
    __privateSet(this, _reader, reader);
  }
  static from(options) {
    return new _a3({ reader: new LocalReader(options) });
  }
  getIdentifier() {
    return __privateGet(this, _reader).getIdentifier();
  }
  getTags() {
    return __privateGet(this, _reader).getTags();
  }
  bytes() {
    return __privateGet(this, _reader).getBytes();
  }
  async text() {
    const bytes = await this.bytes();
    return new TextDecoder().decode(bytes);
  }
  async json() {
    return JSON.parse(await this.text());
  }
}, _reader = new WeakMap(), _a3);

// ../node_modules/@mysten/walrus/dist/files/blob.mjs
var _reader2, _client, _cache, _WalrusBlob_instances, blobStatus_fn, _a4;
var WalrusBlob = (_a4 = class {
  constructor({ reader, client }) {
    __privateAdd(this, _WalrusBlob_instances);
    __privateAdd(this, _reader2);
    __privateAdd(this, _client);
    __privateAdd(this, _cache, new ClientCache());
    __privateSet(this, _reader2, reader);
    __privateSet(this, _client, client);
  }
  asFile() {
    return new WalrusFile({ reader: __privateGet(this, _reader2) });
  }
  async blobId() {
    return __privateGet(this, _reader2).blobId;
  }
  async files(filters = {}) {
    const quiltReader = await __privateGet(this, _reader2).getQuiltReader();
    const index = await quiltReader.readIndex();
    const files = [];
    for (const patch of index) {
      if (filters.ids && !filters.ids.includes(patch.patchId)) continue;
      if (filters.identifiers && !filters.identifiers.includes(patch.identifier)) continue;
      if (filters.tags && !filters.tags.some((tags) => Object.entries(tags).every(([tagName, tagValue]) => patch.tags[tagName] === tagValue))) continue;
      files.push(new WalrusFile({ reader: quiltReader.readerForPatchId(patch.patchId) }));
    }
    return files;
  }
  async exists() {
    const status = await __privateMethod(this, _WalrusBlob_instances, blobStatus_fn).call(this);
    return status.type === "permanent" || status.type === "deletable";
  }
  async storedUntil() {
    const status = await __privateMethod(this, _WalrusBlob_instances, blobStatus_fn).call(this);
    if (status.type === "permanent") return status.endEpoch;
    return null;
  }
}, _reader2 = new WeakMap(), _client = new WeakMap(), _cache = new WeakMap(), _WalrusBlob_instances = new WeakSet(), blobStatus_fn = async function() {
  return __privateGet(this, _cache).read(["blobStatus", __privateGet(this, _reader2).blobId], () => __privateGet(this, _client).getVerifiedBlobStatus({ blobId: __privateGet(this, _reader2).blobId }));
}, _a4);

// ../node_modules/@mysten/walrus/dist/contracts/utils/index.mjs
var MOVE_STDLIB_ADDRESS = normalizeSuiAddress("0x1");
var SUI_FRAMEWORK_ADDRESS = normalizeSuiAddress("0x2");
function getPureBcsSchema(typeTag) {
  const parsedTag = typeof typeTag === "string" ? TypeTagSerializer.parseFromStr(typeTag) : typeTag;
  if ("u8" in parsedTag) return suiBcs.U8;
  else if ("u16" in parsedTag) return suiBcs.U16;
  else if ("u32" in parsedTag) return suiBcs.U32;
  else if ("u64" in parsedTag) return suiBcs.U64;
  else if ("u128" in parsedTag) return suiBcs.U128;
  else if ("u256" in parsedTag) return suiBcs.U256;
  else if ("address" in parsedTag) return suiBcs.Address;
  else if ("bool" in parsedTag) return suiBcs.Bool;
  else if ("vector" in parsedTag) {
    const type = getPureBcsSchema(parsedTag.vector);
    return type ? suiBcs.vector(type) : null;
  } else if ("struct" in parsedTag) {
    const structTag = parsedTag.struct;
    const pkg = normalizeSuiAddress(structTag.address);
    if (pkg === MOVE_STDLIB_ADDRESS) {
      if ((structTag.module === "ascii" || structTag.module === "string") && structTag.name === "String") return suiBcs.String;
      if (structTag.module === "option" && structTag.name === "Option") {
        const inner = structTag.typeParams[0];
        const type = inner ? getPureBcsSchema(inner) : null;
        return type ? suiBcs.option(type) : null;
      }
    }
    if (pkg === SUI_FRAMEWORK_ADDRESS && structTag.module === "object" && (structTag.name === "ID" || structTag.name === "UID")) return suiBcs.Address;
  }
  return null;
}
function normalizeMoveArguments(args, argTypes, parameterNames) {
  const argLen = Array.isArray(args) ? args.length : Object.keys(args).length;
  if (parameterNames && argLen !== parameterNames.length) throw new Error(`Invalid number of arguments, expected ${parameterNames.length}, got ${argLen}`);
  const normalizedArgs = [];
  let index = 0;
  for (const argType of argTypes) {
    if (argType === "0x2::clock::Clock") {
      normalizedArgs.push((tx) => tx.object.clock());
      continue;
    }
    if (argType === "0x2::random::Random") {
      normalizedArgs.push((tx) => tx.object.random());
      continue;
    }
    if (argType === "0x2::deny_list::DenyList") {
      normalizedArgs.push((tx) => tx.object.denyList());
      continue;
    }
    if (argType === "0x3::sui_system::SuiSystemState") {
      normalizedArgs.push((tx) => tx.object.system());
      continue;
    }
    let arg;
    if (Array.isArray(args)) {
      if (index >= args.length) throw new Error(`Invalid number of arguments, expected at least ${index + 1}, got ${args.length}`);
      arg = args[index];
    } else {
      if (!parameterNames) throw new Error(`Expected arguments to be passed as an array`);
      const name = parameterNames[index];
      arg = args[name];
      if (arg === void 0) throw new Error(`Parameter ${name} is required`);
    }
    index += 1;
    if (typeof arg === "function" || isArgument(arg)) {
      normalizedArgs.push(arg);
      continue;
    }
    const bcsType = argType === null ? null : getPureBcsSchema(argType);
    if (bcsType) {
      const bytes = bcsType.serialize(arg);
      normalizedArgs.push((tx) => tx.pure(bytes));
      continue;
    }
    if (typeof arg === "string") {
      normalizedArgs.push((tx) => tx.object(arg));
      continue;
    }
    throw new Error(`Invalid argument ${stringify(arg)} for type ${argType}`);
  }
  return normalizedArgs;
}
var HAS_PHANTOM_REGEX = /phantom [A-Za-z_$][A-Za-z0-9_$]*/;
function splitTopLevelTypeArgs(inner) {
  const parts = [];
  let depth = 0;
  let current = "";
  for (const char of inner) {
    if (char === "," && depth === 0) {
      parts.push(current.trim());
      current = "";
      continue;
    }
    if (char === "<") depth++;
    if (char === ">") depth--;
    current += char;
  }
  if (current) parts.push(current.trim());
  return parts;
}
function buildTypeTag(name, options) {
  const lt = name.indexOf("<");
  const base = lt === -1 ? name : name.slice(0, lt);
  if (base.split("::").length !== 3) throw new Error(`${name} is not a top-level Move type`);
  let result = name;
  if (options == null ? void 0 : options.typeArguments) {
    const baked = lt === -1 ? [] : splitTopLevelTypeArgs(name.slice(lt + 1, -1));
    const supplied = options.typeArguments.map((arg) => {
      if (typeof arg === "string") return arg;
      if (arg && typeof arg.serialize === "function" && typeof arg.name === "string") return arg.name;
      throw new Error(`Invalid type argument ${stringify(arg)}`);
    });
    if (supplied.length !== baked.length) throw new Error(`Expected ${baked.length} type arguments for ${base}, got ${supplied.length}`);
    result = supplied.length === 0 ? base : `${base}<${supplied.join(", ")}>`;
  }
  if (HAS_PHANTOM_REGEX.test(result)) throw new Error((options == null ? void 0 : options.typeArguments) ? `A type argument contains an unfilled phantom parameter in ${result}` : `Missing type arguments for ${result}`);
  if (options == null ? void 0 : options.package) {
    const [, ...rest] = result.split("::");
    result = [options.package, ...rest].join("::");
  }
  if (!HAS_PHANTOM_REGEX.test(result) && !/[@/]/.test(result)) TypeTagSerializer.parseFromStr(result);
  return result;
}
async function resolveBuiltTypeTag(name, options) {
  const { client, ...rest } = options;
  const { type } = await client.core.mvr.resolveType({ type: buildTypeTag(name, rest) });
  return normalizeStructTag(type);
}
var MoveStruct = class extends BcsStruct {
  /**
  * Build the type tag for this struct.
  *
  * `typeArguments` is the full positional list, in Move declaration order, and
  * is required when the struct has unfilled phantom parameters. The result may
  * contain MVR names: those are valid in transaction `typeArguments`, but for
  * queries or comparisons against on-chain data use `resolveTypeTag` instead.
  */
  typeTag(...args) {
    return buildTypeTag(this.name, args[0]);
  }
  /**
  * Build the type tag for this struct, then resolve any MVR names through the
  * client (using its configured overrides and the MVR API) and return the
  * normalized, address-only form suitable for queries and comparisons against
  * on-chain data.
  */
  async resolveTypeTag(options) {
    return resolveBuiltTypeTag(this.name, options);
  }
  async get({ objectId, ...options }) {
    const [res] = await this.getMany({
      ...options,
      objectIds: [objectId]
    });
    if (!res) throw new Error(`No object found for id ${objectId}`);
    return res;
  }
  async getMany({ client, ...options }) {
    return (await client.core.getObjects({
      ...options,
      include: {
        ...options.include,
        content: true
      }
    })).objects.map((obj) => {
      if (obj instanceof Error) throw obj;
      return {
        ...obj,
        json: this.parse(obj.content)
      };
    });
  }
};
var MoveEnum = class extends BcsEnum {
  /** Build the type tag for this enum. See `MoveStruct.typeTag` for semantics. */
  typeTag(...args) {
    return buildTypeTag(this.name, args[0]);
  }
  /** Build and resolve the type tag for this enum. See `MoveStruct.resolveTypeTag`. */
  async resolveTypeTag(options) {
    return resolveBuiltTypeTag(this.name, options);
  }
};
var MoveTuple = class extends BcsTuple {
  /** Build the type tag for this struct. See `MoveStruct.typeTag` for semantics. */
  typeTag(...args) {
    return buildTypeTag(this.name, args[0]);
  }
  /** Build and resolve the type tag for this struct. See `MoveStruct.resolveTypeTag`. */
  async resolveTypeTag(options) {
    return resolveBuiltTypeTag(this.name, options);
  }
};
function stringify(val) {
  if (typeof val === "object") return JSON.stringify(val, (_key, value) => typeof value === "bigint" ? value.toString() : value);
  if (typeof val === "bigint") return val.toString();
  return val;
}

// ../node_modules/@mysten/walrus/dist/contracts/walrus/storage_resource.mjs
var $moduleName = "@local-pkg/walrus::storage_resource";
var Storage = new MoveStruct({
  name: `${$moduleName}::Storage`,
  fields: {
    id: suiBcs.Address,
    start_epoch: suiBcs.u32(),
    end_epoch: suiBcs.u32(),
    storage_size: suiBcs.u64()
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/blob.mjs
var $moduleName2 = "@local-pkg/walrus::blob";
var Blob = new MoveStruct({
  name: `${$moduleName2}::Blob`,
  fields: {
    id: suiBcs.Address,
    registered_epoch: suiBcs.u32(),
    blob_id: suiBcs.u256(),
    size: suiBcs.u64(),
    encoding_type: suiBcs.u8(),
    certified_epoch: suiBcs.option(suiBcs.u32()),
    storage: Storage,
    deletable: suiBcs.bool()
  }
});
var BlobIdDerivation = new MoveStruct({
  name: `${$moduleName2}::BlobIdDerivation`,
  fields: {
    encoding_type: suiBcs.u8(),
    size: suiBcs.u64(),
    root_hash: suiBcs.u256()
  }
});
function addMetadata(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [null, null];
  const parameterNames = ["self", "metadata"];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "blob",
    function: "add_metadata",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}
function insertOrUpdateMetadataPair(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [
    null,
    "0x1::string::String",
    "0x1::string::String"
  ];
  const parameterNames = [
    "self",
    "key",
    "value"
  ];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "blob",
    function: "insert_or_update_metadata_pair",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}
function removeMetadataPair(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [null, "0x1::string::String"];
  const parameterNames = ["self", "key"];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "blob",
    function: "remove_metadata_pair",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}

// ../node_modules/@mysten/walrus/dist/contracts/walrus/deps/sui/vec_map.mjs
var $moduleName3 = "0x2::vec_map";
function Entry(...typeParameters) {
  return new MoveStruct({
    name: `${$moduleName3}::Entry<${typeParameters[0].name}, ${typeParameters[1].name}>`,
    fields: {
      key: typeParameters[0],
      value: typeParameters[1]
    }
  });
}
function VecMap(...typeParameters) {
  return new MoveStruct({
    name: `${$moduleName3}::VecMap<${typeParameters[0].name}, ${typeParameters[1].name}>`,
    fields: { contents: suiBcs.vector(Entry(typeParameters[0], typeParameters[1])) }
  });
}

// ../node_modules/@mysten/walrus/dist/contracts/walrus/metadata.mjs
var $moduleName4 = "@local-pkg/walrus::metadata";
var Metadata = new MoveStruct({
  name: `${$moduleName4}::Metadata`,
  fields: { metadata: VecMap(suiBcs.string(), suiBcs.string()) }
});
function _new(options = {}) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "metadata",
    function: "new"
  });
}

// ../node_modules/@mysten/walrus/dist/contracts/walrus/deps/sui/object_table.mjs
var $moduleName5 = "0x2::object_table";
var ObjectTable = new MoveStruct({
  name: `${$moduleName5}::ObjectTable<phantom K, phantom V>`,
  fields: {
    id: suiBcs.Address,
    size: suiBcs.u64()
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/extended_field.mjs
var $moduleName6 = "@local-pkg/walrus::extended_field";
var ExtendedField = new MoveStruct({
  name: `${$moduleName6}::ExtendedField<phantom T>`,
  fields: { id: suiBcs.Address }
});
var Key = new MoveTuple({
  name: `${$moduleName6}::Key`,
  fields: [suiBcs.bool()]
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/committee.mjs
var $moduleName7 = "@local-pkg/walrus::committee";
var Committee = new MoveTuple({
  name: `${$moduleName7}::Committee`,
  fields: [VecMap(suiBcs.Address, suiBcs.vector(suiBcs.u16()))]
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/epoch_parameters.mjs
var $moduleName8 = "@local-pkg/walrus::epoch_parameters";
var EpochParams = new MoveStruct({
  name: `${$moduleName8}::EpochParams`,
  fields: {
    total_capacity_size: suiBcs.u64(),
    storage_price_per_unit_size: suiBcs.u64(),
    write_price_per_unit_size: suiBcs.u64()
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/staking_inner.mjs
var $moduleName9 = "@local-pkg/walrus::staking_inner";
var EpochState = new MoveEnum({
  name: `${$moduleName9}::EpochState`,
  fields: {
    EpochChangeSync: suiBcs.u16(),
    EpochChangeDone: suiBcs.u64(),
    NextParamsSelected: suiBcs.u64()
  }
});
var StakingInnerV1 = new MoveStruct({
  name: `${$moduleName9}::StakingInnerV1`,
  fields: {
    n_shards: suiBcs.u16(),
    epoch_duration: suiBcs.u64(),
    first_epoch_start: suiBcs.u64(),
    pools: ObjectTable,
    epoch: suiBcs.u32(),
    active_set: ExtendedField,
    next_committee: suiBcs.option(Committee),
    committee: Committee,
    previous_committee: Committee,
    next_epoch_params: suiBcs.option(EpochParams),
    epoch_state: EpochState,
    next_epoch_public_keys: ExtendedField
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/deps/sui/group_ops.mjs
var $moduleName10 = "0x2::group_ops";
var Element = new MoveStruct({
  name: `${$moduleName10}::Element<phantom T>`,
  fields: { bytes: suiBcs.vector(suiBcs.u8()) }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/event_blob.mjs
var $moduleName11 = "@local-pkg/walrus::event_blob";
var EventBlobAttestation = new MoveStruct({
  name: `${$moduleName11}::EventBlobAttestation`,
  fields: {
    checkpoint_sequence_num: suiBcs.u64(),
    epoch: suiBcs.u32()
  }
});
var EventBlob = new MoveStruct({
  name: `${$moduleName11}::EventBlob`,
  fields: {
    blob_id: suiBcs.u256(),
    ending_checkpoint_sequence_number: suiBcs.u64()
  }
});
var EventBlobCertificationState = new MoveStruct({
  name: `${$moduleName11}::EventBlobCertificationState`,
  fields: {
    latest_certified_blob: suiBcs.option(EventBlob),
    aggregate_weight_per_blob: VecMap(EventBlob, suiBcs.u16())
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/storage_node.mjs
var $moduleName12 = "@local-pkg/walrus::storage_node";
var StorageNodeInfo = new MoveStruct({
  name: `${$moduleName12}::StorageNodeInfo`,
  fields: {
    name: suiBcs.string(),
    node_id: suiBcs.Address,
    network_address: suiBcs.string(),
    public_key: Element,
    next_epoch_public_key: suiBcs.option(Element),
    network_public_key: suiBcs.vector(suiBcs.u8()),
    metadata: ExtendedField
  }
});
var StorageNodeCap = new MoveStruct({
  name: `${$moduleName12}::StorageNodeCap`,
  fields: {
    id: suiBcs.Address,
    node_id: suiBcs.Address,
    last_epoch_sync_done: suiBcs.u32(),
    last_event_blob_attestation: suiBcs.option(EventBlobAttestation),
    deny_list_root: suiBcs.u256(),
    deny_list_sequence: suiBcs.u64(),
    deny_list_size: suiBcs.u64()
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/pending_values.mjs
var $moduleName13 = "@local-pkg/walrus::pending_values";
var PendingValues = new MoveTuple({
  name: `${$moduleName13}::PendingValues`,
  fields: [VecMap(suiBcs.u32(), suiBcs.u64())]
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/deps/sui/table.mjs
var $moduleName14 = "0x2::table";
var Table = new MoveStruct({
  name: `${$moduleName14}::Table<phantom K, phantom V>`,
  fields: {
    id: suiBcs.Address,
    size: suiBcs.u64()
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/deps/sui/balance.mjs
var $moduleName15 = "0x2::balance";
var Balance = new MoveStruct({
  name: `${$moduleName15}::Balance<phantom T>`,
  fields: { value: suiBcs.u64() }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/auth.mjs
var $moduleName16 = "@local-pkg/walrus::auth";
var Authenticated = new MoveEnum({
  name: `${$moduleName16}::Authenticated`,
  fields: {
    Sender: suiBcs.Address,
    Object: suiBcs.Address
  }
});
var Authorized = new MoveEnum({
  name: `${$moduleName16}::Authorized`,
  fields: {
    Address: suiBcs.Address,
    ObjectID: suiBcs.Address
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/deps/sui/bag.mjs
var $moduleName17 = "0x2::bag";
var Bag = new MoveStruct({
  name: `${$moduleName17}::Bag`,
  fields: {
    id: suiBcs.Address,
    size: suiBcs.u64()
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/staking_pool.mjs
var $moduleName18 = "@local-pkg/walrus::staking_pool";
var NewEpochCommissionBlockedForCollection = new MoveStruct({
  name: `${$moduleName18}::NewEpochCommissionBlockedForCollection`,
  fields: { dummy_field: suiBcs.bool() }
});
var VotingParams = new MoveStruct({
  name: `${$moduleName18}::VotingParams`,
  fields: {
    storage_price: suiBcs.u64(),
    write_price: suiBcs.u64(),
    node_capacity: suiBcs.u64()
  }
});
var PoolState = new MoveEnum({
  name: `${$moduleName18}::PoolState`,
  fields: {
    Active: null,
    Withdrawing: suiBcs.u32(),
    Withdrawn: null
  }
});
var StakingPool = new MoveStruct({
  name: `${$moduleName18}::StakingPool`,
  fields: {
    id: suiBcs.Address,
    state: PoolState,
    voting_params: VotingParams,
    node_info: StorageNodeInfo,
    activation_epoch: suiBcs.u32(),
    latest_epoch: suiBcs.u32(),
    wal_balance: suiBcs.u64(),
    num_shares: suiBcs.u64(),
    pending_shares_withdraw: PendingValues,
    pre_active_withdrawals: PendingValues,
    pending_commission_rate: PendingValues,
    commission_rate: suiBcs.u16(),
    exchange_rates: Table,
    pending_stake: PendingValues,
    rewards_pool: Balance,
    commission: Balance,
    commission_receiver: Authorized,
    governance_authorized: Authorized,
    extra_fields: Bag
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/staking.mjs
var $moduleName19 = "@local-pkg/walrus::staking";
var Staking = new MoveStruct({
  name: `${$moduleName19}::Staking`,
  fields: {
    id: suiBcs.Address,
    version: suiBcs.u64(),
    package_id: suiBcs.Address,
    new_package_id: suiBcs.option(suiBcs.Address)
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/bls_aggregate.mjs
var $moduleName20 = "@local-pkg/walrus::bls_aggregate";
var BlsCommitteeMember = new MoveStruct({
  name: `${$moduleName20}::BlsCommitteeMember`,
  fields: {
    public_key: Element,
    weight: suiBcs.u16(),
    node_id: suiBcs.Address
  }
});
var BlsCommittee = new MoveStruct({
  name: `${$moduleName20}::BlsCommittee`,
  fields: {
    members: suiBcs.vector(BlsCommitteeMember),
    n_shards: suiBcs.u16(),
    epoch: suiBcs.u32(),
    total_aggregated_key: Element
  }
});
var RequiredWeight = new MoveEnum({
  name: `${$moduleName20}::RequiredWeight`,
  fields: {
    Quorum: null,
    OneCorrectNode: null
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/storage_accounting.mjs
var $moduleName21 = "@local-pkg/walrus::storage_accounting";
var FutureAccounting = new MoveStruct({
  name: `${$moduleName21}::FutureAccounting`,
  fields: {
    epoch: suiBcs.u32(),
    used_capacity: suiBcs.u64(),
    rewards_to_distribute: Balance
  }
});
var FutureAccountingRingBuffer = new MoveStruct({
  name: `${$moduleName21}::FutureAccountingRingBuffer`,
  fields: {
    current_index: suiBcs.u32(),
    length: suiBcs.u32(),
    ring_buffer: suiBcs.vector(FutureAccounting)
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/system_state_inner.mjs
var $moduleName22 = "@local-pkg/walrus::system_state_inner";
var SystemStateInnerV1 = new MoveStruct({
  name: `${$moduleName22}::SystemStateInnerV1`,
  fields: {
    committee: BlsCommittee,
    total_capacity_size: suiBcs.u64(),
    used_capacity_size: suiBcs.u64(),
    storage_price_per_unit_size: suiBcs.u64(),
    write_price_per_unit_size: suiBcs.u64(),
    future_accounting: FutureAccountingRingBuffer,
    event_blob_certification_state: EventBlobCertificationState,
    deny_list_sizes: ExtendedField
  }
});

// ../node_modules/@mysten/walrus/dist/contracts/walrus/system.mjs
var $moduleName23 = "@local-pkg/walrus::system";
var System = new MoveStruct({
  name: `${$moduleName23}::System`,
  fields: {
    id: suiBcs.Address,
    version: suiBcs.u64(),
    package_id: suiBcs.Address,
    new_package_id: suiBcs.option(suiBcs.Address)
  }
});
function reserveSpace(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [
    null,
    "u64",
    "u32",
    null
  ];
  const parameterNames = [
    "self",
    "storageAmount",
    "epochsAhead",
    "payment"
  ];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "system",
    function: "reserve_space",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}
function registerBlob(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [
    null,
    null,
    "u256",
    "u256",
    "u64",
    "u8",
    "bool",
    null
  ];
  const parameterNames = [
    "self",
    "storage",
    "blobId",
    "rootHash",
    "size",
    "encodingType",
    "deletable",
    "writePayment"
  ];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "system",
    function: "register_blob",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}
function certifyBlob(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [
    null,
    null,
    "vector<u8>",
    "vector<u8>",
    "vector<u8>"
  ];
  const parameterNames = [
    "self",
    "blob",
    "signature",
    "signersBitmap",
    "message"
  ];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "system",
    function: "certify_blob",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}
function deleteBlob(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [null, null];
  const parameterNames = ["self", "blob"];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "system",
    function: "delete_blob",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}
function extendBlob(options) {
  const packageAddress = options.package ?? "@local-pkg/walrus";
  const argumentsTypes = [
    null,
    null,
    "u32",
    null
  ];
  const parameterNames = [
    "self",
    "blob",
    "extendedEpochs",
    "payment"
  ];
  return (tx) => tx.moveCall({
    package: packageAddress,
    module: "system",
    function: "extend_blob",
    arguments: normalizeMoveArguments(options.arguments, argumentsTypes, parameterNames)
  });
}

// ../node_modules/@mysten/walrus/dist/storage-node/utils.mjs
function mergeHeaders(...headers) {
  const mergedHeaders = new Headers();
  for (const header of headers) {
    if (!header || typeof header !== "object") continue;
    for (const [key, value] of Object.entries(header)) if (value === null) mergedHeaders.delete(key);
    else if (Array.isArray(value)) for (const v of value) mergedHeaders.append(key, v);
    else if (value !== void 0) mergedHeaders.set(key, value);
  }
  return mergedHeaders;
}

// ../node_modules/@mysten/walrus/dist/storage-node/client.mjs
var _fetch, _timeout, _onError, _StorageNodeClient_instances, request_fn, _a5;
var StorageNodeClient = (_a5 = class {
  constructor({ fetch: overriddenFetch, timeout, onError } = {}) {
    __privateAdd(this, _StorageNodeClient_instances);
    __privateAdd(this, _fetch);
    __privateAdd(this, _timeout);
    __privateAdd(this, _onError);
    __privateSet(this, _fetch, overriddenFetch ?? globalThis.fetch);
    __privateSet(this, _timeout, timeout ?? 3e4);
    __privateSet(this, _onError, onError);
  }
  /**
  * Gets the metadata associated with a Walrus blob.
  */
  async getBlobMetadata({ blobId }, options) {
    const bcsBytes = await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/metadata`, {
      ...options,
      headers: mergeHeaders({ Accept: "application/octet-stream" }, options.headers)
    })).arrayBuffer();
    return BlobMetadataWithId.parse(new Uint8Array(bcsBytes));
  }
  /**
  * Gets the status associated with a Walrus blob.
  */
  async getBlobStatus({ blobId }, options) {
    const blobStatus = (await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/status`, options)).json()).success.data;
    if (blobStatus === "nonexistent") return { type: "nonexistent" };
    if ("invalid" in blobStatus) return {
      type: "invalid",
      ...blobStatus.invalid
    };
    if ("permanent" in blobStatus) return {
      type: "permanent",
      ...blobStatus.permanent
    };
    if ("deletable" in blobStatus) return {
      type: "deletable",
      ...blobStatus.deletable
    };
    throw new StorageNodeError(`Unknown blob status received: ${blobStatus}`);
  }
  /**
  * Stores the metadata associated with a registered Walrus blob at this storage
  * node. This is a pre-requisite for storing the encoded slivers of the blob. The
  * ID of the blob must first be registered on Sui, after which storing the metadata
  * becomes possible.
  *
  * This endpoint may return an error if the node has not yet received the
  * registration event from the chain.
  */
  async storeBlobMetadata({ blobId, metadata }, options) {
    const body = typeof metadata === "object" && "V1" in metadata ? BlobMetadata.serialize(metadata).toBytes() : metadata;
    return await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/metadata`, {
      ...options,
      method: "PUT",
      body,
      headers: mergeHeaders({ "Content-Type": "application/octet-stream" }, options.headers)
    })).json();
  }
  /**
  * Gets the primary or secondary sliver identified by the specified blob ID and
  * index. The index should represent a sliver that is assigned to be stored at one
  * of the shards managed by this storage node during this epoch.
  */
  async getSliver({ blobId, sliverPairIndex, sliverType }, options) {
    const bcsBytes = await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/slivers/${sliverPairIndex}/${sliverType}`, {
      ...options,
      headers: mergeHeaders({ Accept: "application/octet-stream" }, options.headers)
    })).arrayBuffer();
    return new Uint8Array(bcsBytes);
  }
  /**
  * Stores a primary or secondary blob sliver at the storage node.
  */
  async storeSliver({ blobId, sliverPairIndex, sliverType, sliver }, options) {
    const body = typeof sliver === "object" && "symbols" in sliver ? SliverData.serialize(sliver).toBytes() : sliver;
    return await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/slivers/${sliverPairIndex}/${sliverType}`, {
      ...options,
      method: "PUT",
      body,
      headers: mergeHeaders({ "Content-Type": "application/octet-stream" }, options.headers)
    })).json();
  }
  /**
  * Gets a signed storage confirmation from this storage node, indicating that all shards
  * assigned to this storage node for the current epoch have stored their respective slivers.
  */
  async getDeletableBlobConfirmation({ blobId, objectId }, options) {
    return await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/confirmation/deletable/${objectId}`, options)).json();
  }
  /**
  * Gets a signed storage confirmation from this storage node, indicating that all shards
  * assigned to this storage node for the current epoch have stored their respective slivers.
  */
  async getPermanentBlobConfirmation({ blobId }, options) {
    return await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/confirmation/permanent`, options)).json();
  }
  /**
  * Gets the storage status of a sliver at this storage node.
  * Returns whether the sliver is stored, buffered (pending registration), or nonexistent.
  */
  async getSliverStatus({ blobId, sliverPairIndex, sliverType }, options) {
    return (await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/slivers/${sliverPairIndex}/${sliverType}/status`, options)).json()).success.data;
  }
  /**
  * Gets the storage status of blob metadata at this storage node.
  * Returns whether the metadata is stored, buffered (pending registration), or nonexistent.
  */
  async getMetadataStatus({ blobId }, options) {
    return (await (await __privateMethod(this, _StorageNodeClient_instances, request_fn).call(this, `/v1/blobs/${blobId}/metadata/status`, options)).json()).success.data;
  }
}, _fetch = new WeakMap(), _timeout = new WeakMap(), _onError = new WeakMap(), _StorageNodeClient_instances = new WeakSet(), request_fn = async function(path, options) {
  var _a12, _b, _c;
  const { nodeUrl, signal, timeout, ...init2 } = options;
  if (signal == null ? void 0 : signal.aborted) throw new UserAbortError();
  const timeoutSignal = AbortSignal.timeout(timeout ?? __privateGet(this, _timeout));
  let response;
  try {
    const fetch = __privateGet(this, _fetch);
    response = await fetch(`${nodeUrl}${path}`, {
      ...init2,
      signal: signal ? AbortSignal.any([timeoutSignal, signal]) : timeoutSignal
    });
  } catch (error) {
    if (signal == null ? void 0 : signal.aborted) throw new UserAbortError();
    if (error instanceof Error && error.name === "AbortError") {
      const error$1 = new ConnectionTimeoutError();
      (_a12 = __privateGet(this, _onError)) == null ? void 0 : _a12.call(this, error$1);
      throw error$1;
    }
    (_b = __privateGet(this, _onError)) == null ? void 0 : _b.call(this, error);
    throw error;
  }
  if (!response.ok) {
    const errorText = await response.text().catch((reason) => reason);
    const errorJSON = safeParseJSON(errorText);
    const errorMessage = errorJSON ? void 0 : errorText;
    const error = StorageNodeAPIError.generate(response.status, errorJSON, errorMessage);
    (_c = __privateGet(this, _onError)) == null ? void 0 : _c.call(this, error);
    throw error;
  }
  return response;
}, _a5);
function safeParseJSON(value) {
  try {
    return JSON.parse(value);
  } catch {
    return;
  }
}

// ../node_modules/@mysten/walrus/dist/utils/object-loader.mjs
var import_dataloader = __toESM(require_dataloader(), 1);
var _dynamicFieldCache, _a6;
var SuiObjectDataLoader = (_a6 = class extends import_dataloader.default {
  constructor(suiClient) {
    super(async (ids) => {
      const { objects } = await suiClient.core.getObjects({
        objectIds: ids,
        include: { content: true }
      });
      return objects;
    });
    __privateAdd(this, _dynamicFieldCache, /* @__PURE__ */ new Map());
  }
  async load(id, schema) {
    const data = await super.load(id);
    if (schema) return schema.parse(data.content);
    return data;
  }
  async loadMany(ids, schema) {
    const data = await super.loadMany(ids);
    if (!schema) return data;
    return data.map((d) => {
      if (d instanceof Error) return d;
      return schema.parse(d.content);
    });
  }
  async loadManyOrThrow(ids, schema) {
    const data = await this.loadMany(ids, schema);
    for (const d of data) if (d instanceof Error) throw d;
    return data;
  }
  clearAll() {
    __privateGet(this, _dynamicFieldCache).clear();
    return super.clearAll();
  }
  clear(key) {
    __privateGet(this, _dynamicFieldCache).delete(key);
    return super.clear(key);
  }
  async loadFieldObject(parent, name, type) {
    const schema = pureBcsSchemaFromTypeName(name.type);
    const id = deriveDynamicFieldID(parent, "u64", schema.serialize(name.value).toBytes());
    return (await this.load(id, Field(schema, type))).value;
  }
}, _dynamicFieldCache = new WeakMap(), _a6);

// ../node_modules/@mysten/walrus/dist/utils/randomness.mjs
function weightedShuffle(arr) {
  return arr.map(({ value, weight }) => ({
    value,
    weight: Math.pow(Math.random(), 1 / weight)
  })).sort((a, b) => b.weight - a.weight).map((item) => item.value);
}
function shuffle(arr) {
  const result = [...arr];
  for (let i = result.length - 1; i > 0; i -= 1) {
    const j = Math.floor(Math.random() * (i + 1));
    [result[i], result[j]] = [result[j], result[i]];
  }
  return result;
}

// ../node_modules/@mysten/walrus/dist/wasm.mjs
import init, { BlobEncoder, bls12381_min_pk_aggregate, bls12381_min_pk_verify } from "@mysten/walrus-wasm";
async function getWasmBindings(url) {
  await init({ module_or_path: url });
  function encodeBlob(nShards, bytes, encodingType = "RS2") {
    const encoder = new BlobEncoder(nShards);
    if (encodingType !== "RS2") throw new Error(`Unsupported encoding type: ${encodingType}`);
    const bufferSizes = computeBcsBufferSizes(bytes.length, nShards);
    const primaryBuffers = Array.from({ length: nShards }).map(() => new Uint8Array(bufferSizes.primary));
    const secondaryBuffers = Array.from({ length: nShards }).map(() => new Uint8Array(bufferSizes.secondary));
    const [metadata, rootHash] = encoder.encode(bytes, primaryBuffers, secondaryBuffers);
    return {
      primarySlivers: primaryBuffers,
      secondarySlivers: secondaryBuffers,
      blobId: blobIdFromBytes(new Uint8Array(metadata.blob_id)),
      metadata: metadata.metadata,
      rootHash: new Uint8Array(rootHash.Digest)
    };
  }
  function combineSignatures(confirmations, signerIndices) {
    const signature = bls12381_min_pk_aggregate(confirmations.map((confirmation) => fromBase64(confirmation.signature)));
    return {
      signers: signerIndices,
      serializedMessage: fromBase64(confirmations[0].serializedMessage),
      signature
    };
  }
  function decodePrimarySlivers(blobId, nShards, size, slivers, encodingType = "RS2") {
    const encoder = new BlobEncoder(nShards);
    if (encodingType !== "RS2") throw new Error(`Unsupported encoding type: ${encodingType}`);
    const blobSize = BigInt(size);
    const outputBuffer = new Uint8Array(Number(blobSize));
    encoder.decode(BlobId.serialize(blobId).toBytes(), blobSize, slivers, outputBuffer);
    return outputBuffer;
  }
  function getVerifySignature() {
    return (confirmation, publicKey) => bls12381_min_pk_verify(fromBase64(confirmation.signature), publicKey, fromBase64(confirmation.serializedMessage));
  }
  function computeMetadata(nShards, bytes, encodingType = "RS2") {
    const [blobId, rootHash, unencodedLength, encType] = new BlobEncoder(nShards).compute_metadata(bytes);
    if (encodingType !== "RS2") throw new Error(`Unsupported encoding type: ${encodingType}`);
    return {
      blobId: blobIdFromBytes(new Uint8Array(blobId)),
      rootHash: new Uint8Array(rootHash.Digest),
      unencodedLength: BigInt(unencodedLength),
      encodingType: encType
    };
  }
  return {
    encodeBlob,
    combineSignatures,
    decodePrimarySlivers,
    getVerifySignature,
    computeMetadata
  };
}
function uleb128Size(value) {
  let size = 1;
  value >>= 7;
  while (value !== 0) {
    size++;
    value >>= 7;
  }
  return size;
}
function computeBcsBufferSize(dataLength) {
  return uleb128Size(dataLength) + dataLength + 2 + 2;
}
function computeBcsBufferSizes(blobSize, nShards) {
  const { primarySymbols, secondarySymbols } = getSourceSymbols(nShards);
  let symbolSize = Math.floor((Math.max(blobSize, 1) - 1) / (primarySymbols * secondarySymbols)) + 1;
  if (symbolSize % 2 === 1) symbolSize = symbolSize + 1;
  const primarySliverSize = secondarySymbols * symbolSize;
  const secondarySliverSize = primarySymbols * symbolSize;
  return {
    nShards,
    primary: computeBcsBufferSize(primarySliverSize),
    secondary: computeBcsBufferSize(secondarySliverSize)
  };
}

// ../node_modules/@mysten/walrus/dist/upload-relay/client.mjs
var _fetch2, _timeout2, _onError2, _UploadRelayClient_instances, request_fn2, _a7;
var UploadRelayClient = (_a7 = class {
  constructor({ host, fetch: overriddenFetch, timeout, onError }) {
    __privateAdd(this, _UploadRelayClient_instances);
    __privateAdd(this, _fetch2);
    __privateAdd(this, _timeout2);
    __privateAdd(this, _onError2);
    this.host = host;
    __privateSet(this, _fetch2, overriddenFetch ?? globalThis.fetch);
    __privateSet(this, _timeout2, timeout ?? 3e4);
    __privateSet(this, _onError2, onError);
  }
  async tipConfig() {
    const data = await (await __privateMethod(this, _UploadRelayClient_instances, request_fn2).call(this, {
      method: "GET",
      path: "/v1/tip-config"
    })).json();
    if (typeof data === "string") return null;
    if ("const" in data.send_tip.kind) return {
      address: data.send_tip.address,
      kind: { const: data.send_tip.kind.const }
    };
    return {
      address: data.send_tip.address,
      kind: { linear: {
        base: data.send_tip.kind.linear.base,
        perEncodedKib: data.send_tip.kind.linear.encoded_size_mul_per_kib
      } }
    };
  }
  async writeBlob({ blobId, nonce, txDigest, blob, deletable, blobObjectId, requiresTip, encodingType, ...options }) {
    const query = new URLSearchParams({ blob_id: blobId });
    if (requiresTip) {
      query.set("nonce", urlSafeBase64(nonce));
      query.set("tx_id", txDigest);
    }
    if (deletable) query.set("deletable_blob_object", blobObjectId);
    if (encodingType) query.set("encoding_type", encodingType);
    const data = await (await __privateMethod(this, _UploadRelayClient_instances, request_fn2).call(this, {
      method: "POST",
      path: `/v1/blob-upload-relay?${query.toString()}`,
      body: blob,
      ...options
    })).json();
    return {
      blobId,
      certificate: {
        signers: data.confirmation_certificate.signers,
        serializedMessage: new Uint8Array(data.confirmation_certificate.serialized_message),
        signature: fromUrlSafeBase64(data.confirmation_certificate.signature)
      }
    };
  }
}, _fetch2 = new WeakMap(), _timeout2 = new WeakMap(), _onError2 = new WeakMap(), _UploadRelayClient_instances = new WeakSet(), request_fn2 = async function(options) {
  var _a12, _b, _c;
  const { signal, timeout, ...init2 } = options;
  if (signal == null ? void 0 : signal.aborted) throw new UserAbortError();
  const timeoutSignal = AbortSignal.timeout(timeout ?? __privateGet(this, _timeout2));
  let response;
  try {
    const fetch = __privateGet(this, _fetch2);
    response = await fetch(`${this.host}${options.path}`, {
      ...init2,
      signal: signal ? AbortSignal.any([timeoutSignal, signal]) : timeoutSignal
    });
  } catch (error) {
    if (signal == null ? void 0 : signal.aborted) throw new UserAbortError();
    if (error instanceof Error && error.name === "AbortError") {
      const error$1 = new ConnectionTimeoutError();
      (_a12 = __privateGet(this, _onError2)) == null ? void 0 : _a12.call(this, error$1);
      throw error$1;
    }
    (_b = __privateGet(this, _onError2)) == null ? void 0 : _b.call(this, error);
    throw error;
  }
  if (!response.ok) {
    const errorText = await response.text().catch((reason) => reason);
    const errorJSON = safeParseJSON2(errorText);
    const errorMessage = errorJSON ? void 0 : errorText;
    const error = StorageNodeAPIError.generate(response.status, errorJSON, errorMessage);
    (_c = __privateGet(this, _onError2)) == null ? void 0 : _c.call(this, error);
    throw error;
  }
  return response;
}, _a7);
function safeParseJSON2(value) {
  try {
    return JSON.parse(value);
  } catch {
    return;
  }
}

// ../node_modules/@mysten/walrus/dist/files/readers/quilt-file.mjs
var _quilt, _sliverIndex, _identifier2, _tags2, _a8;
var QuiltFileReader = (_a8 = class {
  constructor({ quilt, sliverIndex, identifier, tags }) {
    __privateAdd(this, _quilt);
    __privateAdd(this, _sliverIndex);
    __privateAdd(this, _identifier2);
    __privateAdd(this, _tags2);
    __privateSet(this, _quilt, quilt);
    __privateSet(this, _sliverIndex, sliverIndex);
    __privateSet(this, _identifier2, identifier ?? null);
    __privateSet(this, _tags2, tags);
  }
  async getBytes() {
    const { blobContents, identifier, tags } = await __privateGet(this, _quilt).readBlob(__privateGet(this, _sliverIndex));
    __privateSet(this, _identifier2, identifier);
    __privateSet(this, _tags2, tags ?? {});
    return blobContents;
  }
  async getIdentifier() {
    if (__privateGet(this, _identifier2) !== null) return __privateGet(this, _identifier2);
    __privateSet(this, _identifier2, (await __privateGet(this, _quilt).getBlobHeader(__privateGet(this, _sliverIndex))).identifier);
    return __privateGet(this, _identifier2);
  }
  async getTags() {
    if (__privateGet(this, _tags2) !== void 0) return __privateGet(this, _tags2);
    __privateSet(this, _tags2, (await __privateGet(this, _quilt).getBlobHeader(__privateGet(this, _sliverIndex))).tags ?? {});
    return __privateGet(this, _tags2);
  }
}, _quilt = new WeakMap(), _sliverIndex = new WeakMap(), _identifier2 = new WeakMap(), _tags2 = new WeakMap(), _a8);

// ../node_modules/@mysten/walrus/dist/files/readers/quilt.mjs
var _blob, _cache2, _QuiltReader_instances, readBytesFromSlivers_fn, readBytesFromBlob_fn, readBytes_fn, _a9;
var QuiltReader = (_a9 = class {
  constructor({ blob }) {
    __privateAdd(this, _QuiltReader_instances);
    __privateAdd(this, _blob);
    __privateAdd(this, _cache2, new ClientCache());
    __privateSet(this, _blob, blob);
  }
  async getBlobHeader(sliverIndex) {
    return __privateGet(this, _cache2).read(["getBlobHeader", sliverIndex.toString()], async () => {
      const blobHeader = QuiltPatchBlobHeader.parse(await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, sliverIndex, QUILT_PATCH_BLOB_HEADER_SIZE));
      let offset = QUILT_PATCH_BLOB_HEADER_SIZE;
      let blobSize = blobHeader.length;
      const identifierLength = new DataView((await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, sliverIndex, 2, offset)).buffer).getUint16(0, true);
      blobSize -= 2 + identifierLength;
      offset += 2;
      const identifier = bcs.string().parse(await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, sliverIndex, identifierLength, offset));
      offset += identifierLength;
      let tags = null;
      if (blobHeader.mask & HAS_TAGS_FLAG) {
        const tagsSize = new DataView((await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, sliverIndex, 2, offset)).buffer).getUint16(0, true);
        offset += 2;
        tags = QuiltPatchTags.parse(await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, sliverIndex, tagsSize, offset));
        blobSize -= tagsSize + 2;
        offset += tagsSize;
      }
      return {
        identifier,
        tags,
        blobSize,
        contentOffset: offset
      };
    });
  }
  async readBlob(sliverIndex) {
    const { identifier, tags, blobSize, contentOffset } = await this.getBlobHeader(sliverIndex);
    return {
      identifier,
      tags,
      blobContents: await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, sliverIndex, blobSize, contentOffset)
    };
  }
  readerForPatchId(id) {
    const { quiltId, patchId } = parseQuiltPatchId(id);
    if (quiltId !== __privateGet(this, _blob).blobId) throw new Error(`The requested patch ${patchId} is not part of the quilt ${__privateGet(this, _blob).blobId}`);
    return new QuiltFileReader({
      quilt: this,
      sliverIndex: patchId.startIndex
    });
  }
  async readIndex() {
    const header = new DataView((await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, 0, 5)).buffer);
    const version = header.getUint8(0);
    if (version !== 1) throw new Error(`Unsupported quilt version ${version}`);
    const indexSize = header.getUint32(1, true);
    const indexBytes = await __privateMethod(this, _QuiltReader_instances, readBytes_fn).call(this, 0, indexSize, 5);
    const columnSize = await __privateGet(this, _blob).getColumnSize();
    const indexSlivers = Math.ceil(indexSize / columnSize);
    const index = QuiltIndexV1.parse(indexBytes);
    return index.patches.map((patch, i) => {
      const startIndex = i === 0 ? indexSlivers : index.patches[i - 1].endIndex;
      const reader = new QuiltFileReader({
        quilt: this,
        sliverIndex: startIndex,
        identifier: patch.identifier,
        tags: patch.tags
      });
      return {
        identifier: patch.identifier,
        patchId: urlSafeBase64(QuiltPatchId.serialize({
          quiltId: __privateGet(this, _blob).blobId,
          patchId: {
            version: 1,
            startIndex,
            endIndex: patch.endIndex
          }
        }).toBytes()),
        tags: patch.tags,
        reader
      };
    });
  }
}, _blob = new WeakMap(), _cache2 = new WeakMap(), _QuiltReader_instances = new WeakSet(), readBytesFromSlivers_fn = async function(sliver, length, offset = 0, columnSize) {
  if (!length) return new Uint8Array(0);
  __privateGet(this, _blob).getSecondarySliver({ sliverIndex: sliver }).catch(() => {
  });
  columnSize = columnSize ?? await __privateGet(this, _blob).getColumnSize();
  const columnOffset = Math.floor(offset / columnSize);
  let remainingOffset = offset % columnSize;
  const bytes = new Uint8Array(length);
  let bytesRead = 0;
  const nSlivers = Math.ceil(length / columnSize);
  const slivers = new Array(nSlivers).fill(0).map((_, i) => __privateGet(this, _blob).getSecondarySliver({ sliverIndex: sliver + columnOffset + i }));
  slivers.forEach((p) => p.catch(() => {
  }));
  for (const sliverPromise of slivers) {
    const sliver$1 = await sliverPromise;
    let chunk2 = remainingOffset > 0 ? sliver$1.subarray(remainingOffset) : sliver$1;
    remainingOffset -= chunk2.length;
    if (chunk2.length > length - bytesRead) chunk2 = chunk2.subarray(0, length - bytesRead);
    bytes.set(chunk2, bytesRead);
    bytesRead += chunk2.length;
    if (bytesRead >= length) break;
  }
  return bytes;
}, readBytesFromBlob_fn = async function(startColumn, length, offset = 0) {
  const result = new Uint8Array(length);
  if (!length) return result;
  const blob = await __privateGet(this, _blob).getBytes();
  const [rowSize, symbolSize] = await Promise.all([__privateGet(this, _blob).getRowSize(), __privateGet(this, _blob).getSymbolSize()]);
  const nRows = blob.length / rowSize;
  const symbolsToSkip = Math.floor(offset / symbolSize);
  let remainingOffset = offset % symbolSize;
  let currentCol = startColumn + Math.floor(symbolsToSkip / nRows);
  let currentRow = symbolsToSkip % nRows;
  let bytesRead = 0;
  while (bytesRead < length) {
    const baseIndex = currentRow * rowSize + currentCol * symbolSize;
    const startIndex = baseIndex + remainingOffset;
    const endIndex = Math.min(baseIndex + symbolSize, startIndex + length - bytesRead, blob.length);
    if (startIndex >= blob.length) throw new Error("Index out of bounds");
    const size = endIndex - startIndex;
    for (let i = 0; i < size; i++) result[bytesRead + i] = blob[startIndex + i];
    bytesRead += size;
    remainingOffset = 0;
    currentRow = (currentRow + 1) % nRows;
    if (currentRow === 0) currentCol += 1;
  }
  return result;
}, readBytes_fn = async function(sliver, length, offset = 0, columnSize) {
  if (__privateGet(this, _blob).hasStartedLoadingFullBlob) return __privateMethod(this, _QuiltReader_instances, readBytesFromBlob_fn).call(this, sliver, length, offset);
  try {
    return await __privateMethod(this, _QuiltReader_instances, readBytesFromSlivers_fn).call(this, sliver, length, offset, columnSize);
  } catch {
    return __privateMethod(this, _QuiltReader_instances, readBytesFromBlob_fn).call(this, sliver, length, offset);
  }
}, _a9);

// ../node_modules/@mysten/walrus/dist/files/readers/blob.mjs
var _cache3, _client2, _secondarySlivers, _numShards, _a10;
var BlobReader = (_a10 = class {
  constructor({ client, blobId, numShards }) {
    __privateAdd(this, _cache3, new ClientCache());
    __privateAdd(this, _client2);
    __privateAdd(this, _secondarySlivers, /* @__PURE__ */ new Map());
    __privateAdd(this, _numShards);
    this.hasStartedLoadingFullBlob = false;
    __privateSet(this, _client2, client);
    this.blobId = blobId;
    __privateSet(this, _numShards, numShards);
  }
  async getIdentifier() {
    return null;
  }
  async getTags() {
    return {};
  }
  getQuiltReader() {
    return new QuiltReader({ blob: this });
  }
  async getBytes() {
    return __privateGet(this, _cache3).read(["getBytes"], async () => {
      this.hasStartedLoadingFullBlob = true;
      try {
        return await __privateGet(this, _client2).readBlob({ blobId: this.blobId });
      } catch (error) {
        this.hasStartedLoadingFullBlob = false;
        throw error;
      }
    });
  }
  getMetadata() {
    return __privateGet(this, _cache3).read(["getMetadata"], () => __privateGet(this, _client2).getBlobMetadata({ blobId: this.blobId }));
  }
  async getColumnSize() {
    return __privateGet(this, _cache3).read(["getColumnSize"], async () => {
      const loadingSlivers = [...__privateGet(this, _secondarySlivers).values()];
      if (loadingSlivers.length > 0) {
        const sliver = await Promise.any(loadingSlivers).catch(() => null);
        if (sliver) return sliver.length;
      }
      if (this.hasStartedLoadingFullBlob) {
        const { columnSize: columnSize$1 } = getSizes((await this.getBytes()).length, __privateGet(this, _numShards));
        return columnSize$1;
      }
      const metadata = await this.getMetadata();
      const { columnSize } = getSizes(Number(metadata.metadata.V1.unencoded_length), __privateGet(this, _numShards));
      return columnSize;
    });
  }
  async getSymbolSize() {
    const columnSize = await this.getColumnSize();
    const { primarySymbols } = getSourceSymbols(__privateGet(this, _numShards));
    if (columnSize % primarySymbols !== 0) throw new Error("column size should be divisible by primary symbols");
    return columnSize / primarySymbols;
  }
  async getRowSize() {
    const symbolSize = await this.getSymbolSize();
    const { secondarySymbols } = getSourceSymbols(__privateGet(this, _numShards));
    return symbolSize * secondarySymbols;
  }
  async getSecondarySliver({ sliverIndex, signal }) {
    if (__privateGet(this, _secondarySlivers).has(sliverIndex)) return __privateGet(this, _secondarySlivers).get(sliverIndex);
    const sliverPromise = __privateGet(this, _client2).getSecondarySliver({
      blobId: this.blobId,
      index: sliverIndex,
      signal
    }).then((sliver) => SliverData.parse(sliver).symbols.data);
    __privateGet(this, _secondarySlivers).set(sliverIndex, sliverPromise);
    try {
      const sliver = await sliverPromise;
      __privateGet(this, _secondarySlivers).set(sliverIndex, sliver);
      return sliver;
    } catch (error) {
      __privateGet(this, _secondarySlivers).delete(sliverIndex);
      throw error;
    }
  }
}, _cache3 = new WeakMap(), _client2 = new WeakMap(), _secondarySlivers = new WeakMap(), _numShards = new WeakMap(), _a10);

// ../node_modules/@mysten/walrus/dist/utils/retry.mjs
async function retry(fn, options) {
  let remaining = options.count ?? 3;
  while (remaining > 0) try {
    remaining -= 1;
    return await fn();
  } catch (error) {
    if (remaining <= 0 || options.condition && !options.condition(error)) throw error;
    if (options.delay) await new Promise((resolve) => setTimeout(resolve, (options.delay ?? 1e3) + (options.jitter ? Math.random() * options.jitter : 0)));
  }
  throw new Error("Retry count exceeded");
}

// ../node_modules/@mysten/walrus/dist/flows/write-blob.mjs
function createWriteBlobFlow(client, ctx, options) {
  const { resume } = options;
  let encodeState;
  let registerState;
  let uploadState;
  let certifyState;
  const resumeBlobObjectId = resume && "blobObjectId" in resume ? resume.blobObjectId : void 0;
  const resumeTxDigest = resume && "txDigest" in resume ? resume.txDigest : void 0;
  const resumeNonce = resume && "nonce" in resume && resume.nonce ? fromBase64(resume.nonce) : void 0;
  const resumeBlobId = resume ? resume.blobId : void 0;
  const encode = async () => {
    if (!encodeState) {
      const { blob } = options;
      const hasUploadRelay = ctx.hasUploadRelay();
      const metadata$1 = hasUploadRelay ? await client.computeBlobMetadata({
        bytes: blob,
        nonce: resumeNonce
      }) : await client.encodeBlob(blob);
      if (resumeBlobId && metadata$1.blobId !== resumeBlobId) throw new WalrusClientError(`Resume blobId mismatch: expected ${resumeBlobId}, got ${metadata$1.blobId}. The blob content may have changed.`);
      encodeState = {
        metadata: metadata$1,
        size: blob.length,
        data: hasUploadRelay ? blob : void 0
      };
    }
    const { metadata, size } = encodeState;
    return {
      step: "encoded",
      blobId: metadata.blobId,
      rootHash: toBase64(metadata.rootHash),
      unencodedSize: size,
      ..."nonce" in metadata ? { nonce: toBase64(metadata.nonce) } : {}
    };
  };
  const register = ({ epochs, deletable, owner, attributes }) => {
    if (!encodeState) throw new Error("encode must be executed before calling register");
    const { metadata, size } = encodeState;
    const transaction = new Transaction();
    transaction.setSenderIfNotSet(owner);
    if (ctx.hasUploadRelay()) {
      const meta = metadata;
      transaction.add(client.sendUploadRelayTip({
        size,
        blobDigest: meta.blobDigest,
        nonce: meta.nonce
      }));
    }
    transaction.transferObjects([client.registerBlob({
      size,
      epochs,
      blobId: metadata.blobId,
      rootHash: metadata.rootHash,
      deletable,
      attributes
    })], owner);
    registerState = {
      ...encodeState,
      deletable
    };
    return transaction;
  };
  const upload = async (options$1) => {
    if (!encodeState) throw new Error("encode must be executed before calling upload");
    const { metadata, data } = encodeState;
    const deletable = (options$1 == null ? void 0 : options$1.deletable) ?? (registerState == null ? void 0 : registerState.deletable) ?? false;
    let blobObject;
    if (resumeBlobObjectId) blobObject = await client.getBlobObject(resumeBlobObjectId);
    else if (options$1 == null ? void 0 : options$1.digest) blobObject = await ctx.getCreatedBlob(options$1.digest);
    else throw new Error("Either resume.blobObjectId or upload digest must be provided");
    let certificate;
    if (ctx.hasUploadRelay()) {
      const meta = metadata;
      certificate = (await client.writeBlobToUploadRelay({
        blobId: metadata.blobId,
        blob: data,
        nonce: meta.nonce,
        txDigest: (options$1 == null ? void 0 : options$1.digest) ?? resumeTxDigest,
        blobObjectId: blobObject.id,
        deletable,
        encodingType: meta.metadata.encodingType,
        signal: options$1 == null ? void 0 : options$1.signal
      })).certificate;
    } else {
      const meta = metadata;
      const confirmations = await client.writeEncodedBlobToNodes({
        blobId: metadata.blobId,
        objectId: blobObject.id,
        metadata: meta.metadata,
        sliversByNode: meta.sliversByNode,
        deletable,
        signal: options$1 == null ? void 0 : options$1.signal
      });
      certificate = await client.certificateFromConfirmations({
        confirmations,
        blobId: metadata.blobId,
        blobObjectId: blobObject.id,
        deletable
      });
    }
    uploadState = {
      blobObject,
      blobId: metadata.blobId,
      deletable,
      certificate
    };
    const txDigest = (options$1 == null ? void 0 : options$1.digest) ?? resumeTxDigest;
    return {
      step: "uploaded",
      blobId: metadata.blobId,
      blobObjectId: blobObject.id,
      ...txDigest ? { txDigest } : {},
      certificate: CertificateBcs.serialize(certificate).toBase64()
    };
  };
  const certify = () => {
    if (!uploadState) throw new Error("upload must be executed before calling certify");
    const { blobObject, blobId, deletable, certificate } = uploadState;
    const transaction = client.certifyBlobTransaction({
      certificate,
      blobId,
      blobObjectId: blobObject.id,
      deletable
    });
    certifyState = {
      blobObject,
      blobId,
      transaction
    };
    return transaction;
  };
  const executeRegister = async ({ signer, ...options$1 }) => {
    const transaction = register(options$1);
    const { digest } = await ctx.executeTransaction(transaction, signer, "register blob");
    return {
      step: "registered",
      blobId: encodeState.metadata.blobId,
      blobObjectId: (await ctx.getCreatedBlob(digest)).id,
      txDigest: digest,
      ..."nonce" in encodeState.metadata ? { nonce: toBase64(encodeState.metadata.nonce) } : {}
    };
  };
  const executeCertify = async ({ signer }) => {
    const transaction = certify();
    await ctx.executeTransaction(transaction, signer, "certify blob");
    const blobObject = await ctx.loadBlobObject(uploadState.blobObject.id);
    return {
      step: "certified",
      blobId: uploadState.blobId,
      blobObjectId: blobObject.id,
      blobObject
    };
  };
  const getBlob = async () => {
    if (!certifyState && !uploadState) throw new Error("upload or certify must be executed before calling getBlob");
    const state = certifyState ?? uploadState;
    const blobObject = await ctx.loadBlobObject(state.blobObject.id);
    return {
      step: "certified",
      blobId: state.blobId,
      blobObjectId: blobObject.id,
      blobObject
    };
  };
  const restoreUploadState = async (resumeData, deletable) => {
    uploadState = {
      blobObject: await client.getBlobObject(resumeData.blobObjectId),
      blobId: resumeData.blobId,
      deletable,
      certificate: parseCertificateFromBase64(resumeData.certificate)
    };
  };
  async function* run(options$1) {
    const resumeStep = resume == null ? void 0 : resume.step;
    const stepOrder = [
      "encoded",
      "registered",
      "uploaded",
      "certified"
    ];
    const resumeIndex = resumeStep ? stepOrder.indexOf(resumeStep) : -1;
    if (resumeIndex >= stepOrder.indexOf("certified")) return;
    if (resumeIndex < stepOrder.indexOf("encoded")) yield await encode();
    else if (resumeIndex < stepOrder.indexOf("uploaded")) await encode();
    let registerDigest;
    if (!resumeBlobObjectId) {
      const regResult = await executeRegister({
        signer: options$1.signer,
        epochs: options$1.epochs,
        deletable: options$1.deletable,
        owner: options$1.owner ?? options$1.signer.toSuiAddress(),
        attributes: options$1.attributes
      });
      registerDigest = regResult.txDigest;
      yield regResult;
    } else registerState = {
      ...encodeState,
      deletable: options$1.deletable
    };
    if (resumeIndex < stepOrder.indexOf("uploaded")) yield await upload({
      digest: registerDigest ?? resumeTxDigest,
      signal: options$1.signal
    });
    else await restoreUploadState(resume, options$1.deletable);
    yield await executeCertify({ signer: options$1.signer });
  }
  return {
    encode,
    register,
    upload,
    certify,
    getBlob,
    executeRegister,
    executeCertify,
    run
  };
}

// ../node_modules/@mysten/walrus/dist/flows/write-files.mjs
function createWriteFilesFlow(client, { files, resume }) {
  let quiltBytes;
  let quiltIndex;
  const blobFlow = client.writeBlobFlow({
    get blob() {
      if (!quiltBytes) throw new Error("encode must be executed before accessing blob");
      return quiltBytes;
    },
    resume
  });
  const encode = async () => {
    if (!quiltBytes) {
      const { quilt, index } = await client.encodeQuilt({ blobs: await Promise.all(files.map(async (file, i) => ({
        contents: await file.bytes(),
        identifier: await file.getIdentifier() ?? `file-${i}`,
        tags: await file.getTags() ?? {}
      }))) });
      quiltBytes = quilt;
      quiltIndex = index;
    }
    return blobFlow.encode();
  };
  const register = (options) => {
    if (!quiltBytes) throw new Error("encode must be executed before calling register");
    return blobFlow.register({
      ...options,
      attributes: {
        _walrusBlobType: "quilt",
        ...options.attributes
      }
    });
  };
  const listFiles = async () => {
    if (!quiltIndex) throw new Error("encode must be executed before calling listFiles");
    const certResult = await blobFlow.getBlob();
    return quiltIndex.patches.map((patch) => ({
      id: encodeQuiltPatchId({
        quiltId: certResult.blobId,
        patchId: {
          version: 1,
          startIndex: patch.startIndex,
          endIndex: patch.endIndex
        }
      }),
      blobId: certResult.blobId,
      blobObject: certResult.blobObject
    }));
  };
  async function* run(options) {
    const resumeStep = resume == null ? void 0 : resume.step;
    const stepOrder = [
      "encoded",
      "registered",
      "uploaded",
      "certified"
    ];
    const resumeIndex = resumeStep ? stepOrder.indexOf(resumeStep) : -1;
    if (resumeIndex >= stepOrder.indexOf("certified")) return;
    if (resumeIndex < stepOrder.indexOf("encoded")) yield await encode();
    else if (resumeIndex < stepOrder.indexOf("uploaded")) await encode();
    const resumeBlobObjectId = resume && "blobObjectId" in resume ? resume.blobObjectId : void 0;
    let registerDigest;
    if (!resumeBlobObjectId) {
      const regResult = await blobFlow.executeRegister({
        signer: options.signer,
        epochs: options.epochs,
        deletable: options.deletable,
        owner: options.owner ?? options.signer.toSuiAddress(),
        attributes: {
          _walrusBlobType: "quilt",
          ...options.attributes
        }
      });
      registerDigest = regResult.txDigest;
      yield regResult;
    }
    if (resumeIndex < stepOrder.indexOf("uploaded")) yield await blobFlow.upload({
      digest: registerDigest,
      deletable: options.deletable,
      signal: options.signal
    });
    if (resumeIndex < stepOrder.indexOf("certified")) yield await blobFlow.executeCertify({ signer: options.signer });
  }
  return {
    encode,
    register,
    upload: blobFlow.upload,
    certify: blobFlow.certify,
    listFiles,
    executeRegister: async (options) => {
      const { signer, ...rest } = options;
      return blobFlow.executeRegister({
        signer,
        ...rest,
        attributes: {
          _walrusBlobType: "quilt",
          ...rest.attributes
        }
      });
    },
    executeCertify: blobFlow.executeCertify,
    run
  };
}

// ../node_modules/@mysten/walrus/dist/client.mjs
function walrus({ packageConfig, name = "walrus", ...options } = {}) {
  return {
    name,
    register: (client) => {
      const walrusNetwork = client.network;
      if (walrusNetwork !== "mainnet" && walrusNetwork !== "testnet") throw new WalrusClientError("Walrus client only supports mainnet and testnet");
      return new WalrusClient(packageConfig ? {
        packageConfig,
        suiClient: client,
        ...options
      } : {
        network: walrusNetwork,
        suiClient: client,
        ...options
      });
    }
  };
}
var _storageNodeClient, _storageNodeUrlScheme, _wasmUrl, _packageConfig, _suiClient, _objectLoader, _blobMetadataConcurrencyLimit, _readCommittee, _cache4, _uploadRelayConfig, _uploadRelayClient, _WalrusClient_instances, walType_fn, getPackageId_fn, getWalrusPackageId_fn, wasmBindings_fn, internalReadBlob_fn, getCertificationEpoch_fn, getReadCommittee_fn, forceGetReadCommittee_fn, storageCostFromEncodedSize_fn, withWal_fn, loadTipConfig_fn, getCreatedBlob_fn, writeBlobAttributesForRef_fn, writeSliversWithStatusCheck_fn, executeTransaction_fn, getCommittee_fn, getActiveCommittee_fn, stakingPool_fn, getNodeByShardIndex_fn, retryOnPossibleEpochChange_fn, flowContext_fn, _a11;
var WalrusClient = (_a11 = class {
  constructor(config) {
    __privateAdd(this, _WalrusClient_instances);
    __privateAdd(this, _storageNodeClient);
    __privateAdd(this, _storageNodeUrlScheme);
    __privateAdd(this, _wasmUrl);
    __privateAdd(this, _packageConfig);
    __privateAdd(this, _suiClient);
    __privateAdd(this, _objectLoader);
    __privateAdd(this, _blobMetadataConcurrencyLimit, 10);
    __privateAdd(this, _readCommittee);
    __privateAdd(this, _cache4);
    __privateAdd(this, _uploadRelayConfig, null);
    __privateAdd(this, _uploadRelayClient, null);
    this.readBlob = __privateMethod(this, _WalrusClient_instances, retryOnPossibleEpochChange_fn).call(this, __privateMethod(this, _WalrusClient_instances, internalReadBlob_fn));
    this.getSecondarySliver = __privateMethod(this, _WalrusClient_instances, retryOnPossibleEpochChange_fn).call(this, this.internalGetSecondarySliver);
    if (config.network && !config.packageConfig) {
      const network = config.network;
      switch (network) {
        case "testnet":
          __privateSet(this, _packageConfig, TESTNET_WALRUS_PACKAGE_CONFIG);
          break;
        case "mainnet":
          __privateSet(this, _packageConfig, MAINNET_WALRUS_PACKAGE_CONFIG);
          break;
        default:
          throw new WalrusClientError(`Unsupported network: ${network}`);
      }
    } else __privateSet(this, _packageConfig, config.packageConfig);
    __privateSet(this, _wasmUrl, config.wasmUrl);
    __privateSet(this, _storageNodeUrlScheme, config.storageNodeUrlScheme ?? "https");
    __privateSet(this, _uploadRelayConfig, config.uploadRelay ?? null);
    if (__privateGet(this, _uploadRelayConfig)) __privateSet(this, _uploadRelayClient, new UploadRelayClient(__privateGet(this, _uploadRelayConfig)));
    __privateSet(this, _suiClient, config.suiClient);
    __privateSet(this, _storageNodeClient, new StorageNodeClient(config.storageNodeClientOptions));
    __privateSet(this, _objectLoader, new SuiObjectDataLoader(__privateGet(this, _suiClient)));
    __privateSet(this, _cache4, __privateGet(this, _suiClient).cache.scope("@mysten/walrus"));
  }
  /** The Move type for a Blob object */
  getBlobType() {
    return __privateGet(this, _cache4).read(["getBlobType"], async () => {
      return `${await __privateMethod(this, _WalrusClient_instances, getPackageId_fn).call(this)}::blob::Blob`;
    });
  }
  /** The cached system object for the walrus package */
  systemObject() {
    return __privateGet(this, _objectLoader).load(__privateGet(this, _packageConfig).systemObjectId, System);
  }
  /** The cached staking pool object for the walrus package */
  stakingObject() {
    return __privateGet(this, _objectLoader).load(__privateGet(this, _packageConfig).stakingPoolId, Staking);
  }
  /** The system state for the current version of walrus contract  */
  async systemState() {
    return await __privateGet(this, _objectLoader).loadFieldObject(__privateGet(this, _packageConfig).systemObjectId, {
      type: "u64",
      value: (await this.systemObject()).version
    }, SystemStateInnerV1);
  }
  /** The staking state for the current version of walrus contract */
  async stakingState() {
    return __privateGet(this, _objectLoader).loadFieldObject(__privateGet(this, _packageConfig).stakingPoolId, {
      type: "u64",
      value: (await this.stakingObject()).version
    }, StakingInnerV1);
  }
  async computeBlobMetadata({ bytes, numShards, nonce: providedNonce }) {
    let shardCount;
    if (typeof numShards === "number") shardCount = numShards;
    else shardCount = (await this.systemState()).committee.n_shards;
    const { blobId, rootHash, unencodedLength, encodingType } = (await __privateMethod(this, _WalrusClient_instances, wasmBindings_fn).call(this)).computeMetadata(shardCount, bytes);
    let sha256Hash;
    const nonce = providedNonce ?? crypto.getRandomValues(new Uint8Array(32));
    return {
      rootHash,
      blobId,
      metadata: {
        encodingType,
        unencodedLength
      },
      nonce,
      blobDigest: () => {
        if (!sha256Hash) sha256Hash = crypto.subtle.digest("SHA-256", bytes).then((hash) => new Uint8Array(hash));
        return sha256Hash;
      }
    };
  }
  async getBlobMetadata({ blobId, signal }) {
    const randomizedNodes = shuffle((await __privateMethod(this, _WalrusClient_instances, getReadCommittee_fn).call(this, {
      blobId,
      signal
    })).nodes);
    const numShards = (await this.stakingState()).n_shards;
    let numNotFoundWeight = 0;
    let numBlockedWeight = 0;
    let totalErrorCount = 0;
    const controller = new AbortController();
    const metadataExecutors = randomizedNodes.map((node) => async () => {
      try {
        return await __privateGet(this, _storageNodeClient).getBlobMetadata({ blobId }, {
          nodeUrl: node.networkUrl,
          signal: signal ? AbortSignal.any([controller.signal, signal]) : controller.signal
        });
      } catch (error) {
        if (error instanceof NotFoundError) numNotFoundWeight += node.shardIndices.length;
        else if (error instanceof LegallyUnavailableError) numBlockedWeight += node.shardIndices.length;
        totalErrorCount += 1;
        throw error;
      }
    });
    try {
      return await metadataExecutors.shift()();
    } catch {
      const chunkedExecutors = chunk(metadataExecutors, Math.floor(metadataExecutors.length / __privateGet(this, _blobMetadataConcurrencyLimit)));
      return await new Promise((resolve, reject) => {
        chunkedExecutors.forEach(async (executors) => {
          for (const executor of executors) try {
            const result = await executor();
            controller.abort("Blob metadata successfully retrieved.");
            resolve(result);
          } catch (error) {
            if (error instanceof UserAbortError) {
              reject(error);
              return;
            } else if (isQuorum(numBlockedWeight + numNotFoundWeight, numShards)) {
              const abortError = numNotFoundWeight > numBlockedWeight ? new BlobNotCertifiedError(`The specified blob ${blobId} is not certified.`) : new BlobBlockedError(`The specified blob ${blobId} is blocked.`);
              controller.abort(abortError);
              reject(abortError);
              return;
            }
            if (totalErrorCount === metadataExecutors.length) reject(new NoBlobMetadataReceivedError("No valid blob metadata could be retrieved from any storage node."));
          }
        });
      });
    }
  }
  async internalGetSecondarySliver({ blobId, index, signal }) {
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    const numShards = (await this.stakingState()).n_shards;
    const sliverPairIndex = sliverPairIndexFromSecondarySliverIndex(index, numShards);
    const shardIndex = toShardIndex(sliverPairIndex, blobId, numShards);
    const node = await __privateMethod(this, _WalrusClient_instances, getNodeByShardIndex_fn).call(this, committee, shardIndex);
    if (!node) throw new Error(`No node found for shard index ${shardIndex}`);
    return await __privateGet(this, _storageNodeClient).getSliver({
      blobId,
      sliverPairIndex,
      sliverType: "secondary"
    }, {
      nodeUrl: node.networkUrl,
      signal
    });
  }
  async getSlivers({ blobId, signal }) {
    const randomizedNodes = weightedShuffle((await __privateMethod(this, _WalrusClient_instances, getReadCommittee_fn).call(this, {
      blobId,
      signal
    })).nodes.map((node) => ({
      value: node,
      weight: node.shardIndices.length
    })));
    const numShards = (await this.stakingState()).n_shards;
    const { primarySymbols: minSymbols } = getSourceSymbols(numShards);
    const sliverPairIndices = randomizedNodes.flatMap((node) => node.shardIndices.map((shardIndex) => ({
      url: node.networkUrl,
      sliverPairIndex: toPairIndex(shardIndex, blobId, numShards)
    })));
    const controller = new AbortController();
    const chunkedSliverPairIndices = chunk(sliverPairIndices, minSymbols);
    const slivers = [];
    const failedNodes = /* @__PURE__ */ new Set();
    let numNotFoundWeight = 0;
    let numBlockedWeight = 0;
    let totalErrorCount = 0;
    return new Promise((resolve, reject) => {
      chunkedSliverPairIndices[0].forEach(async (_, colIndex) => {
        var _a12;
        for (let rowIndex = 0; rowIndex < chunkedSliverPairIndices.length; rowIndex += 1) {
          const value = (_a12 = chunkedSliverPairIndices.at(rowIndex)) == null ? void 0 : _a12.at(colIndex);
          if (!value) break;
          const { url, sliverPairIndex } = value;
          try {
            if (failedNodes.has(url)) throw new Error(`Skipping node at ${url} due to previous failure.`);
            const sliver = await __privateGet(this, _storageNodeClient).getSliver({
              blobId,
              sliverPairIndex,
              sliverType: "primary"
            }, {
              nodeUrl: url,
              signal: signal ? AbortSignal.any([controller.signal, signal]) : controller.signal
            });
            if (slivers.length === minSymbols) {
              controller.abort("Enough slivers successfully retrieved.");
              resolve(slivers);
              return;
            }
            slivers.push(sliver);
          } catch (error) {
            if (error instanceof NotFoundError) numNotFoundWeight += 1;
            else if (error instanceof LegallyUnavailableError) numBlockedWeight += 1;
            else if (error instanceof UserAbortError) {
              reject(error);
              return;
            }
            if (isQuorum(numBlockedWeight + numNotFoundWeight, numShards)) {
              const abortError = numNotFoundWeight > numBlockedWeight ? new BlobNotCertifiedError(`The specified blob ${blobId} is not certified.`) : new BlobBlockedError(`The specified blob ${blobId} is blocked.`);
              controller.abort(abortError);
              reject(abortError);
              return;
            }
            failedNodes.add(url);
            totalErrorCount += 1;
            const remainingTasks = sliverPairIndices.length - (slivers.length + totalErrorCount);
            if (slivers.length + remainingTasks < minSymbols) {
              const abortError = new NotEnoughSliversReceivedError(`Unable to retrieve enough slivers to decode blob ${blobId}.`);
              controller.abort(abortError);
              reject(abortError);
            }
          }
        }
      });
    });
  }
  /**
  * Gets the blob status from multiple storage nodes and returns the latest status that can be verified.
  */
  async getVerifiedBlobStatus({ blobId, signal }) {
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    const numShards = (await this.stakingState()).n_shards;
    const controller = new AbortController();
    const sortedStatuses = [...(await new Promise((resolve, reject) => {
      const results = [];
      let successWeight = 0;
      let numNotFoundWeight = 0;
      let settledCount = 0;
      committee.nodes.forEach(async (node) => {
        const weight = node.shardIndices.length;
        try {
          const status = await __privateGet(this, _storageNodeClient).getBlobStatus({ blobId }, {
            nodeUrl: node.networkUrl,
            signal: signal ? AbortSignal.any([controller.signal, signal]) : controller.signal
          });
          if (isQuorum(successWeight, numShards)) {
            controller.abort("Quorum of blob statuses retrieved successfully.");
            resolve(results);
          } else {
            successWeight += weight;
            results.push({
              status,
              weight
            });
          }
        } catch (error) {
          if (error instanceof NotFoundError) numNotFoundWeight += weight;
          else if (error instanceof UserAbortError) reject(error);
          if (isQuorum(numNotFoundWeight, numShards)) {
            const abortError = new BlobNotCertifiedError("The blob does not exist.");
            controller.abort(abortError);
            reject(abortError);
          }
        } finally {
          settledCount += 1;
          if (settledCount === committee.nodes.length) reject(new NoBlobStatusReceivedError("Not enough statuses were retrieved to achieve quorum."));
        }
      });
    })).reduce((accumulator, value) => {
      const { status, weight } = value;
      const key = JSON.stringify(status);
      const existing = accumulator.get(key);
      if (existing) existing.totalWeight += weight;
      else accumulator.set(key, {
        status,
        totalWeight: weight
      });
      return accumulator;
    }, /* @__PURE__ */ new Map()).values()].toSorted((a, b) => statusLifecycleRank[b.status.type] - statusLifecycleRank[a.status.type]);
    for (const value of sortedStatuses) if (isAboveValidity(value.totalWeight, numShards)) return value.status;
    throw new NoVerifiedBlobStatusReceivedError(`The blob status could not be verified for blob ${blobId},`);
  }
  /**
  * Calculate the cost of storing a blob for a given a size and number of epochs.
  */
  async storageCost(size, epochs) {
    const encodedSize = encodedBlobLength(size, (await this.systemState()).committee.n_shards);
    return __privateMethod(this, _WalrusClient_instances, storageCostFromEncodedSize_fn).call(this, encodedSize, epochs);
  }
  /**
  * A utility for creating a storage object in a transaction.
  *
  * @example
  * ```ts
  * tx.transferObjects([client.createStorage({ size: 1000, epochs: 3 })], owner);
  * ```
  */
  createStorage({ size, epochs, walCoin }) {
    return async (tx) => {
      const systemObject = await this.systemObject();
      const encodedSize = encodedBlobLength(size, (await this.systemState()).committee.n_shards);
      const [{ storageCost }, walrusPackageId] = await Promise.all([this.storageCost(size, epochs), __privateMethod(this, _WalrusClient_instances, getWalrusPackageId_fn).call(this)]);
      return tx.add(__privateMethod(this, _WalrusClient_instances, withWal_fn).call(this, storageCost, walCoin ?? null, (coin, tx$1) => {
        return tx$1.add(reserveSpace({
          package: walrusPackageId,
          arguments: {
            self: systemObject.id,
            storageAmount: encodedSize,
            epochsAhead: epochs,
            payment: coin
          }
        }));
      }));
    };
  }
  /**
  * Create a transaction that creates a storage object
  *
  * @example
  * ```ts
  * const tx = client.createStorageTransaction({ size: 1000, epochs: 3, owner: signer.toSuiAddress() });
  * ```
  */
  createStorageTransaction({ transaction = new Transaction(), size, epochs, owner }) {
    transaction.transferObjects([this.createStorage({
      size,
      epochs
    })], owner);
    return transaction;
  }
  /**
  * Execute a transaction that creates a storage object
  *
  * @example
  * ```ts
  * const { digest, storage } = await client.executeCreateStorageTransaction({ size: 1000, epochs: 3, signer });
  * ```
  */
  async executeCreateStorageTransaction({ signer, ...options }) {
    var _a12;
    const transaction = this.createStorageTransaction({
      ...options,
      owner: ((_a12 = options.transaction) == null ? void 0 : _a12.getData().sender) ?? signer.toSuiAddress()
    });
    const blobType = await this.getBlobType();
    const { digest, effects } = await __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, transaction, signer, "create storage");
    const createdObjectIds = effects == null ? void 0 : effects.changedObjects.filter((object) => object.idOperation === "Created").map((object) => object.objectId);
    const suiBlobObject = (await __privateGet(this, _suiClient).core.getObjects({
      objectIds: createdObjectIds,
      include: { content: true }
    })).objects.find((object) => !(object instanceof Error) && object.type === blobType);
    if (suiBlobObject instanceof Error || !suiBlobObject) throw new WalrusClientError(`Storage object not found in transaction effects for transaction (${digest})`);
    return {
      digest,
      storage: Storage.parse(suiBlobObject.content)
    };
  }
  /**
  * Register a blob in a transaction
  *
  * @example
  * ```ts
  * tx.transferObjects([client.registerBlob({ size: 1000, epochs: 3, blobId, rootHash, deletable: true })], owner);
  * ```
  */
  registerBlob({ size, epochs, blobId, rootHash, deletable, walCoin, attributes }) {
    return async (tx) => {
      const { writeCost } = await this.storageCost(size, epochs);
      const walrusPackageId = await __privateMethod(this, _WalrusClient_instances, getWalrusPackageId_fn).call(this);
      return tx.add(__privateMethod(this, _WalrusClient_instances, withWal_fn).call(this, writeCost, walCoin ?? null, async (writeCoin, tx$1) => {
        const blob = tx$1.add(registerBlob({
          package: walrusPackageId,
          arguments: {
            self: tx$1.object(__privateGet(this, _packageConfig).systemObjectId),
            storage: this.createStorage({
              size,
              epochs,
              walCoin
            }),
            blobId: blobIdToInt(blobId),
            rootHash: BigInt(bcs.u256().parse(rootHash)),
            size,
            encodingType: 1,
            deletable,
            writePayment: writeCoin
          }
        }));
        if (attributes) tx$1.add(__privateMethod(this, _WalrusClient_instances, writeBlobAttributesForRef_fn).call(this, {
          attributes,
          existingAttributes: null,
          blob
        }));
        return blob;
      }));
    };
  }
  addAuthPayload({ size, blobDigest, nonce }) {
    return async (transaction) => {
      const nonceDigest = await crypto.subtle.digest("SHA-256", nonce);
      const lengthBytes = bcs.u64().serialize(size).toBytes();
      const digest = typeof blobDigest === "function" ? await blobDigest() : blobDigest;
      const authPayload = new Uint8Array(nonceDigest.byteLength + digest.byteLength + lengthBytes.byteLength);
      authPayload.set(digest, 0);
      authPayload.set(new Uint8Array(nonceDigest), digest.byteLength);
      authPayload.set(lengthBytes, nonceDigest.byteLength + digest.byteLength);
      transaction.pure(authPayload);
    };
  }
  async calculateUploadRelayTip(options) {
    const systemState = await this.systemState();
    const encodedSize = encodedBlobLength(options.size, systemState.committee.n_shards);
    const tipConfig = await __privateMethod(this, _WalrusClient_instances, loadTipConfig_fn).call(this);
    if (!tipConfig) return 0n;
    const { max, kind } = tipConfig;
    const amount = "const" in kind ? kind.const : BigInt(kind.linear.base) + BigInt(kind.linear.perEncodedKib) * ((BigInt(encodedSize) + 1023n) / 1024n);
    if (max != null && amount > max) throw new WalrusClientError(`Tip amount (${amount}) exceeds the maximum allowed tip (${max})`);
    return amount;
  }
  sendUploadRelayTip({ size, blobDigest, nonce }) {
    return async (transaction) => {
      const tipConfig = await __privateMethod(this, _WalrusClient_instances, loadTipConfig_fn).call(this);
      if (tipConfig) {
        transaction.add(this.addAuthPayload({
          size,
          blobDigest,
          nonce
        }));
        const amount = await this.calculateUploadRelayTip({ size });
        const { address } = tipConfig;
        transaction.transferObjects([coinWithBalance({ balance: amount })], address);
      }
    };
  }
  /**
  * Create a transaction that registers a blob
  *
  * @example
  * ```ts
  * const tx = client.registerBlobTransaction({ size: 1000, epochs: 3, blobId, rootHash, deletable: true });
  * ```
  */
  registerBlobTransaction({ transaction = new Transaction(), ...options }) {
    const registration = transaction.add(this.registerBlob(options));
    transaction.transferObjects([registration], options.owner);
    return transaction;
  }
  /**
  * Execute a transaction that registers a blob
  *
  * @example
  * ```ts
  * const { digest, blob } = await client.executeRegisterBlobTransaction({ size: 1000, epochs: 3, signer });
  * ```
  */
  async executeRegisterBlobTransaction({ signer, ...options }) {
    var _a12;
    const transaction = this.registerBlobTransaction({
      ...options,
      owner: options.owner ?? ((_a12 = options.transaction) == null ? void 0 : _a12.getData().sender) ?? signer.toSuiAddress()
    });
    const blobType = await this.getBlobType();
    const { digest, effects } = await __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, transaction, signer, "register blob");
    const createdObjectIds = effects == null ? void 0 : effects.changedObjects.filter((object) => object.idOperation === "Created").map((object) => object.objectId);
    const suiBlobObject = (await __privateGet(this, _suiClient).core.getObjects({
      objectIds: createdObjectIds,
      include: { content: true }
    })).objects.find((object) => !(object instanceof Error) && object.type === blobType);
    if (suiBlobObject instanceof Error || !suiBlobObject) throw new WalrusClientError(`Blob object not found in transaction effects for transaction (${digest})`);
    return {
      digest,
      blob: Blob.parse(suiBlobObject.content)
    };
  }
  async certificateFromConfirmations({ confirmations, blobId, deletable, blobObjectId }) {
    const systemState = await this.systemState();
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    if (confirmations.length !== systemState.committee.members.length) throw new WalrusClientError("Invalid number of confirmations. Confirmations array must contain an entry for each node");
    const confirmationMessage = StorageConfirmation.serialize({
      intent: IntentType.BLOB_CERT_MSG,
      epoch: systemState.committee.epoch,
      messageContents: {
        blobId,
        blobType: deletable ? { Deletable: { objectId: blobObjectId } } : { Permanent: null }
      }
    }).toBase64();
    const bindings = await __privateMethod(this, _WalrusClient_instances, wasmBindings_fn).call(this);
    const verifySignature = bindings.getVerifySignature();
    const filteredConfirmations = confirmations.map((confirmation, index) => {
      return (confirmation == null ? void 0 : confirmation.serializedMessage) === confirmationMessage && verifySignature(confirmation, new Uint8Array(committee.nodes[index].info.public_key.bytes)) ? {
        index,
        ...confirmation
      } : null;
    }).filter((confirmation) => confirmation !== null);
    if (!isQuorum(filteredConfirmations.length, systemState.committee.members.length)) throw new NotEnoughBlobConfirmationsError(`Too many invalid confirmations received for blob (${filteredConfirmations.length} of ${systemState.committee.members.length})`);
    return bindings.combineSignatures(filteredConfirmations, filteredConfirmations.map(({ index }) => index));
  }
  /**
  * Certify a blob in a transaction
  *
  * @example
  * ```ts
  * tx.add(client.certifyBlob({ blobId, blobObjectId, confirmations }));
  * ```
  */
  certifyBlob({ blobId, blobObjectId, confirmations, certificate, deletable }) {
    return async (tx) => {
      const systemState = await this.systemState();
      const combinedSignature = (typeof certificate === "string" ? parseCertificateFromBase64(certificate) : certificate) ?? await this.certificateFromConfirmations({
        confirmations,
        blobId,
        deletable,
        blobObjectId
      });
      const walrusPackageId = await __privateMethod(this, _WalrusClient_instances, getWalrusPackageId_fn).call(this);
      tx.add(certifyBlob({
        package: walrusPackageId,
        arguments: {
          self: __privateGet(this, _packageConfig).systemObjectId,
          blob: blobObjectId,
          signature: tx.pure.vector("u8", combinedSignature.signature),
          signersBitmap: tx.pure.vector("u8", signersToBitmap(combinedSignature.signers, systemState.committee.members.length)),
          message: tx.pure.vector("u8", combinedSignature.serializedMessage)
        }
      }));
    };
  }
  /**
  * Create a transaction that certifies a blob
  *
  * @example
  * ```ts
  * const tx = client.certifyBlobTransaction({ blobId, blobObjectId, confirmations });
  * ```
  */
  certifyBlobTransaction({ transaction = new Transaction(), ...options }) {
    transaction.add(this.certifyBlob(options));
    return transaction;
  }
  /**
  * Execute a transaction that certifies a blob
  *
  * @example
  * ```ts
  * const { digest } = await client.executeCertifyBlobTransaction({ blobId, blobObjectId, confirmations, signer });
  * ```
  */
  async executeCertifyBlobTransaction({ signer, ...options }) {
    const transaction = this.certifyBlobTransaction(options);
    const { digest } = await __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, transaction, signer, "certify blob");
    return { digest };
  }
  /**
  * Delete a blob in a transaction
  *
  * @example
  * ```ts
  * const storage = await client.deleteBlob({ blobObjectId });
  * tx.transferObjects([storage], owner);
  * ```
  */
  deleteBlob({ blobObjectId }) {
    return async (tx) => {
      const walrusPackageId = await __privateMethod(this, _WalrusClient_instances, getWalrusPackageId_fn).call(this);
      return tx.add(deleteBlob({
        package: walrusPackageId,
        arguments: {
          self: __privateGet(this, _packageConfig).systemObjectId,
          blob: blobObjectId
        }
      }));
    };
  }
  /**
  * Create a transaction that deletes a blob
  *
  * @example
  * ```ts
  * const tx = client.deleteBlobTransaction({ blobObjectId, owner });
  * ```
  */
  deleteBlobTransaction({ owner, blobObjectId, transaction = new Transaction() }) {
    const storage = transaction.add(this.deleteBlob({ blobObjectId }));
    transaction.transferObjects([storage], owner);
    return transaction;
  }
  /**
  * Execute a transaction that deletes a blob
  *
  * @example
  * ```ts
  * const { digest } = await client.executeDeleteBlobTransaction({ blobObjectId, signer });
  * ```
  */
  async executeDeleteBlobTransaction({ signer, transaction = new Transaction(), blobObjectId }) {
    const { digest } = await __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, this.deleteBlobTransaction({
      blobObjectId,
      transaction,
      owner: transaction.getData().sender ?? signer.toSuiAddress()
    }), signer, "delete blob");
    return { digest };
  }
  /**
  * Extend a blob in a transaction
  *
  * @example
  * ```ts
  * const tx = client.extendBlobTransaction({ blobObjectId, epochs });
  * ```
  */
  extendBlob({ blobObjectId, epochs, endEpoch, walCoin }) {
    return async (tx) => {
      const blob = await __privateGet(this, _objectLoader).load(blobObjectId, Blob);
      const numEpochs = typeof epochs === "number" ? epochs : endEpoch - blob.storage.end_epoch;
      if (numEpochs <= 0) return;
      const { storageCost } = await __privateMethod(this, _WalrusClient_instances, storageCostFromEncodedSize_fn).call(this, Number(blob.storage.storage_size), numEpochs);
      const walrusPackageId = await __privateMethod(this, _WalrusClient_instances, getWalrusPackageId_fn).call(this);
      return tx.add(__privateMethod(this, _WalrusClient_instances, withWal_fn).call(this, storageCost, walCoin ?? null, async (coin, tx$1) => {
        tx$1.add(extendBlob({
          package: walrusPackageId,
          arguments: {
            self: __privateGet(this, _packageConfig).systemObjectId,
            blob: blobObjectId,
            extendedEpochs: numEpochs,
            payment: coin
          }
        }));
      }));
    };
  }
  /**
  * Create a transaction that extends a blob
  *
  * @example
  * ```ts
  * const tx = client.extendBlobTransaction({ blobObjectId, epochs });
  * ```
  */
  async extendBlobTransaction({ transaction = new Transaction(), ...options }) {
    transaction.add(this.extendBlob(options));
    return transaction;
  }
  /**
  * Execute a transaction that extends a blob
  *
  * @example
  * ```ts
  * const { digest } = await client.executeExtendBlobTransaction({ blobObjectId, signer });
  * ```
  */
  async executeExtendBlobTransaction({ signer, ...options }) {
    const { digest } = await __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, await this.extendBlobTransaction(options), signer, "extend blob");
    return { digest };
  }
  async readBlobAttributes({ blobObjectId }) {
    const response = await __privateGet(this, _suiClient).core.getDynamicField({
      parentId: blobObjectId,
      name: {
        type: "vector<u8>",
        bcs: bcs.string().serialize("metadata").toBytes()
      }
    });
    const parsedMetadata = Metadata.parse(response.dynamicField.value.bcs);
    return Object.fromEntries(parsedMetadata.metadata.contents.map(({ key, value }) => [key, value]));
  }
  /**
  * Write attributes to a blob
  *
  * If attributes already exists, their previous values will be overwritten
  * If an attribute is set to `null`, it will be removed from the blob
  *
  * @example
  * ```ts
  * tx.add(client.writeBlobAttributes({ blobObjectId, attributes: { key: 'value', keyToRemove: null } }));
  * ```
  */
  writeBlobAttributes({ blobObject, blobObjectId, attributes }) {
    return async (tx) => {
      const existingAttributes = blobObjectId ? await this.readBlobAttributes({ blobObjectId }) : null;
      const blob = blobObject ?? tx.object(blobObjectId);
      tx.add(__privateMethod(this, _WalrusClient_instances, writeBlobAttributesForRef_fn).call(this, {
        attributes,
        existingAttributes,
        blob
      }));
    };
  }
  /**
  * Create a transaction that writes attributes to a blob
  *
  * If attributes already exists, their previous values will be overwritten
  * If an attribute is set to `null`, it will be removed from the blob
  *
  * @example
  * ```ts
  * const tx = client.writeBlobAttributesTransaction({ blobObjectId, attributes: { key: 'value', keyToRemove: null } });
  * ```
  */
  async writeBlobAttributesTransaction({ transaction = new Transaction(), ...options }) {
    transaction.add(await this.writeBlobAttributes(options));
    return transaction;
  }
  /**
  * Execute a transaction that writes attributes to a blob
  *
  * If attributes already exists, their previous values will be overwritten
  * If an attribute is set to `null`, it will be removed from the blob
  *
  * @example
  * ```ts
  * const { digest } = await client.executeWriteBlobAttributesTransaction({ blobObjectId, signer });
  * ```
  */
  async executeWriteBlobAttributesTransaction({ signer, ...options }) {
    const { digest } = await __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, await this.writeBlobAttributesTransaction(options), signer, "write blob attributes");
    return { digest };
  }
  /**
  * Write a sliver to a storage node
  *
  * @example
  * ```ts
  * const res = await client.writeSliver({ blobId, sliverPairIndex, sliverType, sliver });
  * ```
  */
  async writeSliver({ blobId, sliverPairIndex, sliverType, sliver, signal }) {
    const systemState = await this.systemState();
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    const shardIndex = toShardIndex(sliverPairIndex, blobId, systemState.committee.n_shards);
    const node = await __privateMethod(this, _WalrusClient_instances, getNodeByShardIndex_fn).call(this, committee, shardIndex);
    return __privateGet(this, _storageNodeClient).storeSliver({
      blobId,
      sliverPairIndex,
      sliverType,
      sliver
    }, {
      nodeUrl: node.networkUrl,
      signal
    });
  }
  /**
  * Write metadata to a storage node
  *
  * @example
  * ```ts
  * const res = await client.writeMetadataToNode({ nodeIndex, blobId, metadata });
  * ```
  */
  async writeMetadataToNode({ nodeIndex, blobId, metadata, signal }) {
    const node = (await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this)).nodes[nodeIndex];
    return retry(() => __privateGet(this, _storageNodeClient).storeBlobMetadata({
      blobId,
      metadata
    }, {
      nodeUrl: node.networkUrl,
      signal
    }), {
      count: 3,
      delay: 1e3,
      condition: (error) => error instanceof BlobNotRegisteredError
    });
  }
  /**
  * Get a storage confirmation from a storage node
  *
  * @example
  * ```ts
  * const confirmation = await client.getStorageConfirmationFromNode({ nodeIndex, blobId, deletable, objectId });
  * ```
  */
  async getStorageConfirmationFromNode({ nodeIndex, blobId, deletable, objectId, signal }) {
    var _a12, _b, _c;
    const node = (await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this)).nodes[nodeIndex];
    return ((_c = (_b = (_a12 = deletable ? await __privateGet(this, _storageNodeClient).getDeletableBlobConfirmation({
      blobId,
      objectId
    }, {
      nodeUrl: node.networkUrl,
      signal
    }) : await __privateGet(this, _storageNodeClient).getPermanentBlobConfirmation({ blobId }, {
      nodeUrl: node.networkUrl,
      signal
    })) == null ? void 0 : _a12.success) == null ? void 0 : _b.data) == null ? void 0 : _c.signed) ?? null;
  }
  /**
  * Get storage confirmations from all active committee nodes for a blob.
  *
  * @example
  * ```ts
  * const confirmations = await client.getStorageConfirmations({
  *   blobId: savedState.blobId,
  *   deletable: true,
  *   objectId: savedState.blobObjectId,
  * });
  * const tx = client.certifyBlobTransaction({ blobId, blobObjectId, confirmations, deletable: true });
  * ```
  */
  async getStorageConfirmations({ blobId, deletable, objectId, signal }) {
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    return Promise.all(committee.nodes.map((_, nodeIndex) => this.getStorageConfirmationFromNode({
      nodeIndex,
      blobId,
      ...deletable ? {
        deletable: true,
        objectId
      } : { deletable: false },
      signal
    }).catch(() => null)));
  }
  /**
  * Load a parsed Blob object from chain by its Sui object ID.
  *
  * @example
  * ```ts
  * const blobObject = await client.getBlobObject('0x...');
  * console.log(blobObject.id, blobObject.blob_id);
  * ```
  */
  async getBlobObject(blobObjectId) {
    return __privateGet(this, _objectLoader).load(blobObjectId, Blob);
  }
  /**
  * Encode a blob into slivers for each node
  *
  * @example
  * ```ts
  * const { blobId, metadata, sliversByNode, rootHash } = await client.encodeBlob(blob);
  * ```
  */
  async encodeBlob(blob) {
    const systemState = await this.systemState();
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    const numShards = systemState.committee.n_shards;
    const { blobId, metadata, primarySlivers, secondarySlivers, rootHash } = (await __privateMethod(this, _WalrusClient_instances, wasmBindings_fn).call(this)).encodeBlob(numShards, blob);
    const sliversByNodeMap = /* @__PURE__ */ new Map();
    for (let sliverPairIndex = 0; sliverPairIndex < primarySlivers.length; sliverPairIndex++) {
      const primarySliver = primarySlivers[sliverPairIndex];
      const secondarySliver = secondarySlivers[sliverPairIndex];
      const shardIndex = toShardIndex(sliverPairIndex, blobId, numShards);
      const node = await __privateMethod(this, _WalrusClient_instances, getNodeByShardIndex_fn).call(this, committee, shardIndex);
      if (!sliversByNodeMap.has(node.nodeIndex)) sliversByNodeMap.set(node.nodeIndex, {
        primary: [],
        secondary: []
      });
      sliversByNodeMap.get(node.nodeIndex).primary.push({
        sliverIndex: sliverPairIndex,
        sliverPairIndex,
        shardIndex,
        sliver: primarySliver
      });
      sliversByNodeMap.get(node.nodeIndex).secondary.push({
        sliverIndex: sliverPairIndex,
        sliverPairIndex,
        shardIndex,
        sliver: secondarySliver
      });
    }
    const sliversByNode = new Array();
    for (let i = 0; i < systemState.committee.members.length; i++) sliversByNode.push(sliversByNodeMap.get(i) ?? {
      primary: [],
      secondary: []
    });
    return {
      blobId,
      metadata,
      rootHash,
      sliversByNode
    };
  }
  /**
  * Write slivers to a storage node
  *
  * @example
  * ```ts
  * await client.writeSliversToNode({ blobId, slivers, signal });
  * ```
  */
  async writeSliversToNode({ blobId, slivers, signal }) {
    const controller = new AbortController();
    const combinedSignal = signal ? AbortSignal.any([controller.signal, signal]) : controller.signal;
    const primarySliverWrites = slivers.primary.map(({ sliverPairIndex, sliver }) => {
      return this.writeSliver({
        blobId,
        sliverPairIndex,
        sliverType: "primary",
        sliver,
        signal: combinedSignal
      });
    });
    const secondarySliverWrites = slivers.secondary.map(({ sliverPairIndex, sliver }) => {
      return this.writeSliver({
        blobId,
        sliverPairIndex,
        sliverType: "secondary",
        sliver,
        signal: combinedSignal
      });
    });
    await Promise.all([...primarySliverWrites, ...secondarySliverWrites]).catch((error) => {
      controller.abort(error);
      throw error;
    });
  }
  /**
  * Write a blob to all storage nodes
  *
  * @example
  * ```ts
  * await client.writeEncodedBlobToNodes({ blob, deletable, epochs, signer });
  * ```
  */
  async writeEncodedBlobToNodes({ blobId, metadata, sliversByNode, signal, ...options }) {
    const systemState = await this.systemState();
    const committee = await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
    const controller = new AbortController();
    let failures = 0;
    return await Promise.all(sliversByNode.map((slivers, nodeIndex) => {
      return this.writeEncodedBlobToNode({
        blobId,
        nodeIndex,
        metadata,
        slivers,
        signal: signal ? AbortSignal.any([controller.signal, signal]) : controller.signal,
        ...options
      }).catch(() => {
        failures += committee.nodes[nodeIndex].shardIndices.length;
        if (isAboveValidity(failures, systemState.committee.n_shards)) {
          const error = new NotEnoughBlobConfirmationsError(`Too many failures while writing blob ${blobId} to nodes`);
          controller.abort(error);
          throw error;
        }
        return null;
      });
    }));
  }
  /**
  * Writes a blob to to an upload relay
  *
  * @example
  * ```ts
  * await client.writeBlobToUploadRelay({ blob, deletable, epochs, signer });
  * ```
  */
  async writeBlobToUploadRelay(options) {
    var _a12;
    if (!__privateGet(this, _uploadRelayClient)) throw new WalrusClientError("Upload relay not configured");
    return __privateGet(this, _uploadRelayClient).writeBlob({
      ...options,
      requiresTip: !!((_a12 = __privateGet(this, _uploadRelayConfig)) == null ? void 0 : _a12.sendTip)
    });
  }
  /**
  * Write encoded blob to a storage node
  *
  * @example
  * ```ts
  * const res = await client.writeEncodedBlobToNode({ nodeIndex, blobId, metadata, slivers });
  * ```
  */
  async writeEncodedBlobToNode({ nodeIndex, blobId, metadata, slivers, signal, ...options }) {
    const node = (await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this)).nodes[nodeIndex];
    let metaDataExists = false;
    try {
      const metadataStatus = await __privateGet(this, _storageNodeClient).getMetadataStatus({ blobId }, {
        nodeUrl: node.networkUrl,
        signal
      });
      if (metadataStatus === "Stored" || metadataStatus === "Buffered") metaDataExists = true;
    } catch {
    }
    if (!metaDataExists) await this.writeMetadataToNode({
      nodeIndex,
      blobId,
      metadata,
      signal
    });
    if (!metaDataExists) await this.writeSliversToNode({
      blobId,
      slivers,
      signal,
      nodeIndex
    });
    else await __privateMethod(this, _WalrusClient_instances, writeSliversWithStatusCheck_fn).call(this, {
      blobId,
      slivers,
      signal,
      nodeIndex
    });
    return this.getStorageConfirmationFromNode({
      nodeIndex,
      blobId,
      ...options
    });
  }
  /**
  * Write a blob to all storage nodes
  *
  * @example
  * ```ts
  * const { blobId, blobObject } = await client.writeBlob({ blob, deletable, epochs, signer });
  * ```
  */
  async writeBlob({ blob, deletable, epochs, signer, owner, attributes, onStep, resume, signal }) {
    const flow = this.writeBlobFlow({
      blob,
      resume
    });
    for await (const step of flow.run({
      signer,
      epochs,
      deletable,
      owner: owner ?? signer.toSuiAddress(),
      attributes,
      signal
    })) await (onStep == null ? void 0 : onStep(step));
    const result = await flow.getBlob();
    return {
      blobId: result.blobId,
      blobObject: result.blobObject
    };
  }
  async writeQuilt({ blobs, ...options }) {
    const encoded = await this.encodeQuilt({ blobs });
    const result = await this.writeBlob({
      ...options,
      blob: encoded.quilt,
      attributes: {
        _walrusBlobType: "quilt",
        ...options.attributes
      }
    });
    return {
      ...result,
      index: {
        ...encoded.index,
        patches: encoded.index.patches.map((patch) => ({
          ...patch,
          patchId: encodeQuiltPatchId({
            quiltId: result.blobId,
            patchId: {
              version: 1,
              startIndex: patch.startIndex,
              endIndex: patch.endIndex
            }
          })
        }))
      }
    };
  }
  async encodeQuilt({ blobs }) {
    return encodeQuilt({
      blobs,
      numShards: (await this.systemState()).committee.n_shards
    });
  }
  /**
  * Reset cached data in the client
  *
  * @example
  * ```ts
  * client.reset();
  * ```
  */
  reset() {
    __privateGet(this, _objectLoader).clearAll();
    __privateGet(this, _cache4).clear();
  }
  async getBlob({ blobId }) {
    return new WalrusBlob({
      reader: new BlobReader({
        client: this,
        blobId,
        numShards: (await this.systemState()).committee.n_shards
      }),
      client: this
    });
  }
  async getFiles({ ids }) {
    const readersByBlobId = /* @__PURE__ */ new Map();
    const quiltReadersByBlobId = /* @__PURE__ */ new Map();
    const parsedIds = ids.map((id) => parseWalrusId(id));
    const numShards = (await this.systemState()).committee.n_shards;
    for (const id of parsedIds) {
      const blobId = id.kind === "blob" ? id.id : id.id.quiltId;
      if (!readersByBlobId.has(blobId)) readersByBlobId.set(blobId, new BlobReader({
        client: this,
        blobId,
        numShards
      }));
      if (id.kind === "quiltPatch") {
        if (!quiltReadersByBlobId.has(blobId)) quiltReadersByBlobId.set(blobId, new QuiltReader({ blob: readersByBlobId.get(blobId) }));
      }
    }
    return parsedIds.map((id) => {
      if (id.kind === "blob") return new WalrusFile({ reader: readersByBlobId.get(id.id) });
      return new WalrusFile({ reader: new QuiltFileReader({
        quilt: quiltReadersByBlobId.get(id.id.quiltId),
        sliverIndex: id.id.patchId.startIndex
      }) });
    });
  }
  async writeFiles({ files, onStep, resume, ...options }) {
    const flow = this.writeFilesFlow({
      files,
      resume
    });
    for await (const step of flow.run({
      signer: options.signer,
      epochs: options.epochs,
      deletable: options.deletable,
      owner: options.owner ?? options.signer.toSuiAddress(),
      attributes: options.attributes
    })) await (onStep == null ? void 0 : onStep(step));
    return flow.listFiles();
  }
  /**
  * Create a step-by-step files upload flow with resume support.
  *
  * Encodes files into a quilt blob and delegates to `writeBlobFlow()` internally.
  * Supports the same resume, step methods, and `run()` async iterator as `writeBlobFlow()`.
  *
  * @example
  * ```ts
  * const flow = client.writeFilesFlow({ files });
  * for await (const step of flow.run({ signer, epochs: 3, deletable: true })) {
  *   await db.save(fileId, step);
  * }
  * const fileRefs = await flow.listFiles();
  * ```
  */
  writeFilesFlow(options) {
    return createWriteFilesFlow(this, options);
  }
  /**
  * Create a step-by-step blob upload flow with resume support.
  *
  * Returns an object with individual step methods (`encode`, `register`, `upload`, `certify`)
  * for manual control, convenience methods (`executeRegister`, `executeCertify`) that handle
  * signing, and a `run()` async iterator that executes the full pipeline.
  *
  * Each step returns a `WriteBlobStep` result that can be persisted and passed as `resume`
  * to recover from crashes.
  *
  * @example
  * ```ts
  * // Async iterator with checkpoints:
  * const flow = client.writeBlobFlow({ blob });
  * for await (const step of flow.run({ signer, epochs: 3, deletable: true })) {
  *   await db.save(fileId, step);
  * }
  *
  * // Resume from saved state:
  * const saved = await db.load(fileId);
  * const flow = client.writeBlobFlow({ blob, resume: saved });
  * for await (const step of flow.run({ signer, epochs: 3, deletable: true })) {
  *   await db.save(fileId, step);
  * }
  * ```
  */
  writeBlobFlow(options) {
    return createWriteBlobFlow(this, __privateMethod(this, _WalrusClient_instances, flowContext_fn).call(this), options);
  }
}, _storageNodeClient = new WeakMap(), _storageNodeUrlScheme = new WeakMap(), _wasmUrl = new WeakMap(), _packageConfig = new WeakMap(), _suiClient = new WeakMap(), _objectLoader = new WeakMap(), _blobMetadataConcurrencyLimit = new WeakMap(), _readCommittee = new WeakMap(), _cache4 = new WeakMap(), _uploadRelayConfig = new WeakMap(), _uploadRelayClient = new WeakMap(), _WalrusClient_instances = new WeakSet(), /** The Move type for a WAL coin */
walType_fn = function() {
  return __privateGet(this, _cache4).read(["walType"], async () => {
    var _a12;
    const toStake = (await __privateGet(this, _suiClient).core.getMoveFunction({
      packageId: await __privateMethod(this, _WalrusClient_instances, getPackageId_fn).call(this),
      moduleName: "staking",
      name: "stake_with_pool"
    })).function.parameters[1];
    const toStakeCoin = toStake.body.$kind === "datatype" ? toStake.body.datatype : null;
    const toStakeCoinType = ((_a12 = toStakeCoin == null ? void 0 : toStakeCoin.typeParameters[0]) == null ? void 0 : _a12.$kind) === "datatype" ? toStakeCoin.typeParameters[0] : null;
    if ((toStakeCoinType == null ? void 0 : toStakeCoinType.$kind) !== "datatype") throw new WalrusClientError("WAL type not found");
    return normalizeStructTag(toStakeCoinType.datatype.typeName);
  });
}, getPackageId_fn = function() {
  return __privateGet(this, _cache4).read(["getPackageId"], async () => {
    return parseStructTag((await __privateGet(this, _objectLoader).load(__privateGet(this, _packageConfig).systemObjectId)).type).address;
  });
}, getWalrusPackageId_fn = function() {
  return __privateGet(this, _cache4).read(["getSystemPackageId"], async () => {
    const { package_id } = await this.systemObject();
    return package_id;
  });
}, wasmBindings_fn = function() {
  return __privateGet(this, _cache4).read(["wasmBindings"], async () => {
    return getWasmBindings(__privateGet(this, _wasmUrl));
  });
}, internalReadBlob_fn = async function({ blobId, signal }) {
  const systemState = await this.systemState();
  const numShards = systemState.committee.n_shards;
  const blobMetadata = await this.getBlobMetadata({
    blobId,
    signal
  });
  const slivers = await this.getSlivers({
    blobId,
    signal
  });
  const bindings = await __privateMethod(this, _WalrusClient_instances, wasmBindings_fn).call(this);
  const blobBytes = bindings.decodePrimarySlivers(blobId, numShards, blobMetadata.metadata.V1.unencoded_length, slivers);
  if (bindings.computeMetadata(systemState.committee.n_shards, blobBytes).blobId !== blobId) throw new InconsistentBlobError("The specified blob was encoded incorrectly.");
  return blobBytes;
}, getCertificationEpoch_fn = async function({ blobId, signal }) {
  const stakingState = await this.stakingState();
  const currentEpoch = stakingState.epoch;
  if (stakingState.epoch_state.$kind === "EpochChangeSync") {
    const status = await this.getVerifiedBlobStatus({
      blobId,
      signal
    });
    if (status.type === "nonexistent" || status.type === "invalid") throw new BlobNotCertifiedError(`The specified blob ${blobId} is ${status.type}.`);
    if (typeof status.initialCertifiedEpoch !== "number") throw new BlobNotCertifiedError(`The specified blob ${blobId} is not certified.`);
    if (status.initialCertifiedEpoch > currentEpoch) throw new BehindCurrentEpochError(`The client is at epoch ${currentEpoch} while the specified blob was certified at epoch ${status.initialCertifiedEpoch}.`);
    return status.initialCertifiedEpoch;
  }
  return currentEpoch;
}, getReadCommittee_fn = async function(options) {
  if (!__privateGet(this, _readCommittee)) __privateSet(this, _readCommittee, __privateMethod(this, _WalrusClient_instances, forceGetReadCommittee_fn).call(this, options));
  return __privateGet(this, _readCommittee);
}, forceGetReadCommittee_fn = async function({ blobId, signal }) {
  const stakingState = await this.stakingState();
  const isTransitioning = stakingState.epoch_state.$kind === "EpochChangeSync";
  const certificationEpoch = await __privateMethod(this, _WalrusClient_instances, getCertificationEpoch_fn).call(this, {
    blobId,
    signal
  });
  if (isTransitioning && certificationEpoch < stakingState.epoch) return await __privateMethod(this, _WalrusClient_instances, getCommittee_fn).call(this, stakingState.previous_committee);
  return await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this);
}, storageCostFromEncodedSize_fn = async function(encodedSize, epochs) {
  const systemState = await this.systemState();
  const storageUnits = storageUnitsFromSize(encodedSize);
  const storageCost = BigInt(storageUnits) * BigInt(systemState.storage_price_per_unit_size) * BigInt(epochs);
  const writeCost = BigInt(storageUnits) * BigInt(systemState.write_price_per_unit_size);
  return {
    storageCost,
    writeCost,
    totalCost: storageCost + writeCost
  };
}, withWal_fn = function(amount, source, fn) {
  return async (tx) => {
    const walType = await __privateMethod(this, _WalrusClient_instances, walType_fn).call(this);
    const coin = source ? tx.splitCoins(source, [amount])[0] : tx.add(coinWithBalance({
      balance: amount,
      type: walType
    }));
    const result = await fn(coin, tx);
    tx.moveCall({
      target: "0x2::coin::destroy_zero",
      typeArguments: [walType],
      arguments: [coin]
    });
    return result;
  };
}, loadTipConfig_fn = function() {
  return __privateGet(this, _cache4).read(["upload-relay-tip-config"], async () => {
    var _a12;
    if (!((_a12 = __privateGet(this, _uploadRelayConfig)) == null ? void 0 : _a12.sendTip) || !__privateGet(this, _uploadRelayClient)) return null;
    if ("kind" in __privateGet(this, _uploadRelayConfig).sendTip) return __privateGet(this, _uploadRelayConfig).sendTip;
    const tipConfig = await __privateGet(this, _uploadRelayClient).tipConfig();
    if (!tipConfig) return null;
    return {
      ...tipConfig,
      max: __privateGet(this, _uploadRelayConfig).sendTip.max
    };
  });
}, getCreatedBlob_fn = async function(digest) {
  var _a12;
  const blobType = await this.getBlobType();
  const result = await __privateGet(this, _suiClient).core.waitForTransaction({
    digest,
    include: { effects: true }
  });
  const createdObjectIds = (_a12 = (result.Transaction ?? result.FailedTransaction).effects) == null ? void 0 : _a12.changedObjects.filter((object) => object.idOperation === "Created").map((object) => object.objectId);
  const suiBlobObject = (await __privateGet(this, _suiClient).core.getObjects({
    objectIds: createdObjectIds,
    include: { content: true }
  })).objects.find((object) => !(object instanceof Error) && object.type === blobType);
  if (suiBlobObject instanceof Error || !suiBlobObject) throw new WalrusClientError(`Blob object not found in transaction effects for transaction (${digest})`);
  return Blob.parse(suiBlobObject.content);
}, writeBlobAttributesForRef_fn = function({ attributes, existingAttributes, blob }) {
  return async (tx) => {
    const walrusPackageId = await __privateMethod(this, _WalrusClient_instances, getWalrusPackageId_fn).call(this);
    if (!existingAttributes) tx.add(addMetadata({
      package: walrusPackageId,
      arguments: {
        self: blob,
        metadata: _new({ package: walrusPackageId })
      }
    }));
    Object.keys(attributes).forEach((key) => {
      const value = attributes[key];
      if (value === null) {
        if (existingAttributes && key in existingAttributes) tx.add(removeMetadataPair({
          package: walrusPackageId,
          arguments: {
            self: blob,
            key
          }
        }));
      } else tx.add(insertOrUpdateMetadataPair({
        package: walrusPackageId,
        arguments: {
          self: blob,
          key,
          value
        }
      }));
    });
  };
}, writeSliversWithStatusCheck_fn = async function({ blobId, slivers, signal, nodeIndex }) {
  const node = (await __privateMethod(this, _WalrusClient_instances, getActiveCommittee_fn).call(this)).nodes[nodeIndex];
  const controller = new AbortController();
  const combinedSignal = signal ? AbortSignal.any([controller.signal, signal]) : controller.signal;
  const sliverWrites = [...slivers.primary.map((s) => ({
    ...s,
    sliverType: "primary"
  })), ...slivers.secondary.map((s) => ({
    ...s,
    sliverType: "secondary"
  }))].map(async ({ sliverPairIndex, sliver, sliverType }) => {
    if (sliver.byteLength >= SLIVER_STATUS_CHECK_THRESHOLD) try {
      const status = await __privateGet(this, _storageNodeClient).getSliverStatus({
        blobId,
        sliverPairIndex,
        sliverType
      }, {
        nodeUrl: node.networkUrl,
        signal: combinedSignal
      });
      if (status === "Stored" || status === "Buffered") return;
    } catch {
    }
    return this.writeSliver({
      blobId,
      sliverPairIndex,
      sliverType,
      sliver,
      signal: combinedSignal
    });
  });
  await Promise.all(sliverWrites).catch((error) => {
    controller.abort(error);
    throw error;
  });
}, executeTransaction_fn = async function(transaction, signer, action) {
  var _a12;
  transaction.setSenderIfNotSet(signer.toSuiAddress());
  const result = await signer.signAndExecuteTransaction({
    transaction,
    client: __privateGet(this, _suiClient)
  });
  if (result.FailedTransaction) throw new WalrusClientError(`Failed to ${action} (${result.FailedTransaction.digest}): ${(_a12 = result.FailedTransaction.status.error) == null ? void 0 : _a12.message}`);
  const { digest, effects } = result.Transaction;
  await __privateGet(this, _suiClient).core.waitForTransaction({ digest });
  return {
    digest,
    effects
  };
}, getCommittee_fn = async function(committee) {
  const stakingPool = await __privateMethod(this, _WalrusClient_instances, stakingPool_fn).call(this, committee);
  const shardIndicesByNodeId = getShardIndicesByNodeId(committee);
  const byShardIndex = /* @__PURE__ */ new Map();
  return {
    byShardIndex,
    nodes: stakingPool.map(({ node_info }, nodeIndex) => {
      const shardIndices = shardIndicesByNodeId.get(node_info.node_id) ?? [];
      const node = {
        id: node_info.node_id,
        info: node_info,
        networkUrl: `${__privateGet(this, _storageNodeUrlScheme)}://${node_info.network_address}`,
        shardIndices,
        nodeIndex
      };
      for (const shardIndex of shardIndices) byShardIndex.set(shardIndex, node);
      return node;
    })
  };
}, getActiveCommittee_fn = function() {
  return __privateGet(this, _cache4).read(["getActiveCommittee", __privateGet(this, _storageNodeUrlScheme)], async () => {
    const stakingState = await this.stakingState();
    return __privateMethod(this, _WalrusClient_instances, getCommittee_fn).call(this, stakingState.committee);
  });
}, stakingPool_fn = async function(committee) {
  const nodeIds = committee[0].contents.map((node) => node.key);
  return __privateGet(this, _objectLoader).loadManyOrThrow(nodeIds, StakingPool);
}, getNodeByShardIndex_fn = async function(committeeInfo, index) {
  const node = committeeInfo.byShardIndex.get(index);
  if (!node) throw new WalrusClientError(`Node for shard index ${index} not found`);
  return node;
}, retryOnPossibleEpochChange_fn = function(fn) {
  return (async (...args) => {
    try {
      return await fn.apply(this, args);
    } catch (error) {
      if (error instanceof RetryableWalrusClientError) {
        this.reset();
        return await fn.apply(this, args);
      }
      throw error;
    }
  });
}, flowContext_fn = function() {
  return {
    hasUploadRelay: () => !!__privateGet(this, _uploadRelayClient),
    executeTransaction: (transaction, signer, action) => __privateMethod(this, _WalrusClient_instances, executeTransaction_fn).call(this, transaction, signer, action),
    getCreatedBlob: (digest) => __privateMethod(this, _WalrusClient_instances, getCreatedBlob_fn).call(this, digest),
    loadBlobObject: (objectId) => __privateGet(this, _objectLoader).load(objectId, Blob)
  };
}, _a11);
export {
  AuthenticationError,
  BadRequestError,
  BehindCurrentEpochError,
  BlobBlockedError,
  BlobNotCertifiedError,
  BlobNotRegisteredError,
  ConflictError,
  ConnectionError,
  ConnectionTimeoutError,
  InconsistentBlobError,
  InternalServerError,
  LegallyUnavailableError,
  MAINNET_WALRUS_PACKAGE_CONFIG,
  NoBlobMetadataReceivedError,
  NoBlobStatusReceivedError,
  NoVerifiedBlobStatusReceivedError,
  NotEnoughBlobConfirmationsError,
  NotEnoughSliversReceivedError,
  NotFoundError,
  PermissionDeniedError,
  RateLimitError,
  RetryableWalrusClientError,
  StorageNodeAPIError,
  StorageNodeError,
  TESTNET_WALRUS_PACKAGE_CONFIG,
  UnprocessableEntityError,
  UserAbortError,
  WalrusBlob,
  WalrusClient,
  WalrusClientError,
  WalrusFile,
  blobIdFromInt,
  blobIdToInt,
  encodeQuilt,
  walrus
};
//# sourceMappingURL=@mysten_walrus.js.map
